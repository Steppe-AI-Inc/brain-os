{
  "bug_id": "BUG-002",
  "status_report": "NOT_DEPLOYED",
  "note": "Home PC never sets bug_id status directly — this is a report for the Work-PC QA Director to read and update BUG_QUEUE.json itself, per qa/IMPLEMENTATION_HANDOFF.md's ownership rule. Reporting explicitly as NOT ready for retest — do not spend a retest cycle on this yet.",
  "fix_written_not_deployed": true,
  "branch": "pending/bug-002-edge-function-chat-fabrication-fix (local + pushed to that branch, NOT master, NOT deployed)",
  "why_not_deployed": "The fix touches supabase/functions/sem-ai-command/index.ts. This repo's own .githooks/pre-push blocks any push whose commit range touches supabase/functions/** unless ALLOW_FUNCTIONS_DEPLOY=1 is explicitly set — and Edge Function production deploys require explicit founder authorization before push, same as any DB migration. That authorization has not been given as of this report.",
  "fix_description": "Structural fix per the class_sweep_required note in BUG_QUEUE.json's own BUG-002 entry — not a per-resource patch. Added PAST_COMPLETION_CLAIM_PATTERN / claimsPastCompletionWithNoGrounding, wired into the same work_orders.output persist-condition that already carried claimsFutureActionWithNoPlan (deliberately reusing the existing safety-net mechanism, not a new parallel one).",
  "unit_tested": {
    "path": "qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs",
    "result": "13/13 passing, runnable with plain node (no live deploy needed for this level of test)"
  },
  "behavioral_spec_prepared": "qa/scenarios-runner/chat_must_not_fabricate_approval_decision.md — ready for a live retest of the exact original BUG-002 repro (approve an approval via chat) the moment this is deployed.",
  "ready_for_retest": false,
  "next_step": "Needs founder authorization for ALLOW_FUNCTIONS_DEPLOY=1, then deploy, then this becomes ready for retest.",
  "reported_at": "2026-09-01"
}
