# Acceptance matrix — Factory Node Management + Zero-Touch Auto Enrollment (V1)

- **Part of** the canonical contract (`docs/architecture/features/factory-node-management-auto-enrollment.md` §10). Single writer: the
  DIRECTOR capability.

## When each row runs

**On every frozen candidate SHA**, whichever WO ids the candidate claims, run by a distinct authorized verifier
(`VERIFICATION_SPEC.md`):
- **Candidate rows:** AC-5..AC-12, AC-14, AC-15, AC-16.
- **Rehearsals of the final rows:**
  - **R-1:** AC-1 in a fresh disposable Windows VM with a standard (non-admin) local account, which survives logoff and reboot. It is
    isolated from any live checkout, task or credential, never the acceptance machine, and runs against a disposable plane and local
    function runtimes.
  - **R-2:** AC-2 against the disposable plane.
  - **R-3:** AC-3 on the disposable plane with at least three enrolled test identities that report distinct machine fingerprints (for
    example three disposable VMs), with the campaign applied; or, on one machine, under the tenant default only, with the campaign's
    record-and-fingerprint rule proved by AC-14(i), (m) and (p).
  - **R-4:** AC-4 on the disposable plane, with forced interleavings.

A WO is CERTIFIED only when every row it covers has verifier evidence at that SHA. For a final row, rehearsal evidence counts. For the
live-plane parts of AC-10 and AC-11, a Director observation record counts: it is Director-committed, and the receipt cites it by sha256.
P-8 at a candidate SHA is evidenced by AC-11 (the manifest unchanged on the copy; no new stamp) and WO-10's release-provenance inputs;
AC-13 is its final Director record.

**At final acceptance:** AC-1..AC-4, AC-5(f) repeated on the acceptance machine, then AC-13. AC-13 is the Director's acceptance record:
it is covered by no WO, and WO-10 supplies its evidence inputs. The clean acceptance machine is touched for the first time there.

**Criteria source.** Criteria are read from the Director branch at the Director commit designated for the candidate, never from the
candidate tree.

**Evidence rule** (candidate-repo ledger 218):
- A row is judged only from records that would be **different if the claim were false**: server rows; HTTP status and body, with a
  server read-back; process exit; file hashes; signed-commit verification.
- A row is never judged from an instrument's summary line or an implementer report.
- Every row records: command, principal / persona, input, expected, actual, timestamp, environment, and the candidate or release SHA.

## The rows

| AC | Precondition → action → expected postcondition | Evidence that would differ if false | S / P rows |
|---|---|---|---|
| **AC-1 Zero-touch enrollment** | A clean PC (final: the third laptop; R-1: a disposable clean Windows environment). A Factory admin: Brain OS → Factory → Computers → **Add Computer** → configure the envelope → issue a pairing code. On the PC: download and run `BrainFactorySetup.exe`, enter the code. After that, **no human step**. Then: log off and on, and reboot; kill the runtime process; (R-1) inject an install failure and a refused registration. | Server rows walk the founder's enrollment states to `ALIVE`. The new node identity has its own public-key credential, distinct from every other node's. Its envelope is exactly as configured. Setup ran as a standard user, with no elevation prompt, and installed under the user profile. On the machine, the installed artifact's digest (S-5: the PE Authenticode image hash), computed on the machine, equals the receipt's reproduced digest for its channel (R-1: dev channel; final acceptance: production channel); at final acceptance its release manifest is signed under C-3. After a logoff or a reboot, the runtime starts at the installing user's next logon with no step other than that logon, returns through RECOVERING (every lease it held renewed if still its own, or given up), and is ALIVE, not STALE, within 180 s of the logon. While the user is logged off, the computer derives STALE / OFFLINE and its leases lapse into the certified takeover. A killed runtime is restarted by the watchdog. An injected install failure and a refused registration show `INSTALL_FAILED` / `REGISTRATION_FAILED` in server rows, and a retry with the same credential reaches ALIVE without a new code. The machine has no Git, npm, checkout, PowerShell bootstrap, `runner.env`, CA file, PostgreSQL URL, Supabase credential or legacy task, and no manual role was assigned. | S-2, S-4; P-4 |
| **AC-2 Computers page truth** | Three computers enrolled. An admin opens Brain OS → Factory → Computers. | Every row equals the server row: derived state, envelope, release, heartbeat age from server time. Counts come from aggregate queries. The same holds after a reload and in a fresh session. A node shows STALE after 180 s without a heartbeat and OFFLINE after 30 min, exactly as the server derives it. | P-5 |
| **AC-3 Real three-machine execution** | Final: three ALIVE enrolled computers on the certified release. R-3: three enrolled test identities. Work is dispatched through the product's own path (no harness). | Done runs come from **3 distinct enrolled computers**. At final acceptance these are distinct Add Computer records and machine fingerprints, with the founder operating three physical machines, as observed by the Director; hostnames and fingerprints are self-reported, and that limitation is recorded. Every run is stamped with the certified release. Work sharing a surface runs in disjoint intervals, on different computers where more than one is eligible. Every certification satisfies S-13 against the whole authoring set and, under the campaign, is on a different physical machine from every author. Exactly one done run per work order. No run falls outside its computer's envelope. **Authorship (S-16a):** every commit in the `VERIFICATION_SPEC.md` §3.1 range verifies against the implementer's registered signing key, and no Home-computer run is in the authoring set of a work order whose commits change a path outside the Director-document paths (S-16). | S-13, S-16; P-2, P-6 |
| **AC-4 Revocation and lifecycle inverses** | A computer holds a valid session token and an in-flight run (R-4: with forced interleavings; the revoke is issued between a call's first credential read and its commit, and a rotate races the revoke). The admin revokes its credential. Then, on other computers: rotate, re-pair, drain → resume, archive → restore. Also: drain a draining computer and revoke a revoked credential; two admins race the same transition; a revoke during pairing; an envelope amended and then amended back. | No effect of the revoked credential commits after the revocation commits: the call and the revocation are serialized on the credential row (for example, a share lock per call and an exclusive lock at revoke). An interleaving passes when the revoke waits and commits after the call, or the call is refused with rows unchanged; an interleaving the serialization prevents is not a missing subject. After the revoke commits, **every** S-7 node operation from that computer is refused, including **complete** of the in-flight run and **certify**, whatever its session token. The server rows are unchanged by those attempts. The other computers keep working. The revoked computer's evidence is unchanged. **Rotate:** the old credential is superseded and refused. **Re-pair:** the old credential stays revoked, and a new key is issued. **Drain:** no new claims; **resume** restores them. **Archive:** no work; **restore** requires a re-pair. **Idempotent:** an action already in its target state answers "already" and writes no duplicate row. **Race:** one admin wins atomically, the other gets a state error, and there is a single final state. **Revoke during pairing:** nothing is issued from that code. **Envelope amended back:** the earlier envelope applies again, audited both times. | S-2, S-3; P-1 |
| **AC-5 Release trust root and certified bytes** | A node is offered: (a) one byte changed inside the Authenticode-hashed ranges; (b) unsigned; (c) a revoked key; (d) a revoked release; (e) correctly signed by a key **outside its pinned trust set**; (f) on a **production-channel build** (at candidate stage on a disposable plane; at final acceptance on the acceptance machine, offered locally with nothing written to the plane), a dev-key-signed release; (g) the certified release under the correct key (dev key on a disposable plane; the C-3 key at final acceptance). Also: (h) the verifier rebuilds the artifact from the CERTIFIED SHA; (i) a Factory admin adopts the previous certified release; (j) a published release is revoked while nodes run it; (k) a production-channel build, pointed at a disposable plane that declares itself dev, is offered a dev-key-signed release; (l) a key is added through any Admin API or server path; (m) a superseded certified release is offered without an admin adopt; (n) bytes are appended or changed outside the hashed ranges, including a trust key, a mode flag or an API URL placed in the certificate table. | (a)–(f) are refused **before execution**, by name. A sentinel payload in each rejected artifact would leave a marker if it ran, and no marker appears. (g) runs, and its source SHA / version is recorded on the node, its runs and its checkpoints. (h) Each channel's reproduced digest equals that channel's digest in the candidate's release manifest. Every embedded trust-set entry is read back as (key id, sha256 of the public key), with the channel and trust mode. The live trust set is the committed source input: before C-3 it is empty, so the production channel trusts no key at all; after C-3 the production channel's entries equal exactly the (key id, public-key sha256) pairs of the Director's WO-6 revision, with no other key and no key id shared by two keys. The reproduced digest is the production-channel artifact's PE Authenticode image hash; a dev-channel digest is recorded separately. The dev channel holds only dev keys and exists only on disposable planes. (i) The node moves back to the previous certified release, recorded. (j) Nodes on the revoked release stop claiming, say so, and never downgrade silently. (k) Refused: the trust mode is fixed in the artifact at build time. (l) No enrolled node's trust set changes, and a release signed only by that key is refused. (m) Refused by name, never executed. (n) The digest is unchanged, and the node's trust set, mode, endpoint and behaviour are unchanged. No production key material appears anywhere. | S-5; P-9 |
| **AC-6 Envelope integrity; no authority from hostname or resources** | (a) A node uses its own credential to change its envelope, role or capabilities. (b) A request body claims another tenant, role, node, agent identity or capability. (c) A node with the best resource fitness but no verifier authorization, or with the preferred verifier's hostname, competes for verifier work. (d) A node self-reports a capability its envelope lacks. (e) An admin amends an envelope. | (a) refused. (b) refused or ignored, with a **server read-back** proving nothing changed. (c) and (d) not eligible: a hostname, resource fitness or self-report never makes a node eligible. (e) audited (who, when, from → to) and effective with nobody touching the node. **Can hostname or resource fitness create authority? NO.** | S-1, S-4 |
| **AC-7 Persona matrix** | Positive: a founder / holding_admin in `factory.tenant_admins` (tier as recorded). Negative: hr_finance, company_manager, team_lead, sales, engineer, technician, employee, contractor, investor_viewer, anonymous, a foreign tenant, **an employee who self-updated `profiles.role`**, **a holding_admin who self-updated to founder** (attempting founder-only actions), **a tier-`founder` row whose live role is no longer founder** (attempting founder-only actions), and a founder / holding_admin not in `tenant_admins`. Exercised on the Computers page, every Admin API action, every Node API route and operation, and the plane directly. Every route or operation outside S-7 is probed. | Positive: allowed. Negative: refused by the server operation, with no data and no existence leak. Founder-only actions are refused without tier `founder`. Only the four S-7 session-less routes answer without a credential. No node operation exists outside the S-7 list. Every refusal names its cause. | S-7, S-8, S-9; P-9 |
| **AC-8 Pairing abuse and secret handling** | Replay; expired (at the 15-min TTL); revoked; consumed; wrong tenant; the 6th failed attempt on one locator (the code is then revoked); the 21st attempt from one IP in an hour; the 61st per tenant (unknown locators included); two consumes racing. | Every case is refused (fail closed), with its named reason, and audited. Exactly one consume wins. The IP limit keys on the connecting peer address; a forged `X-Forwarded-For` does not reset it. A source check and a sample of issued codes show a CSPRNG secret of at least 55 bits and random, unique locators, with no counter or time structure. The stored verifier is an HMAC. The catalog, a full database dump and the Edge function source contain **no pepper** and no plain SHA-256 of a code. No secret appears in logs or responses. | S-6, S-12; P-9 |
| **AC-9 Takeover non-regression, one engine** (I A.4 §6, §10; II.7) | On a disposable plane, on a work order that **requires verification**, through the Node API **and** by calling the SQL front doors directly. | The §6 sequence, row by row: A claims and leases → A checkpoints P1 → A's real lease expires → eligible B claims and receives P1 → B resumes unfinished work only → stale A's **renew, checkpoint and completion are each REJECTED** inside the front door (also when called directly) → B completes **exactly once, with no duplicate completed side effects of any kind**, including but not limited to: one completion row, one lock release, one evidence record, one dependent release → surface-lock ownership stays valid → independent certification, in which **A's identity (and, under the campaign, A's machine) is refused as certifier**. The compatibility matrix is complete (register, heartbeat, discover / claim, renew, checkpoint, complete, lock acquire, lock release, takeover / recovery, verification claim, certification). The verifier checks every OLD column against the `69df2f52` source itself, and every row has a passing regression. **One engine:** a static check shows that every Edge handler only authenticates and calls one front door, and a mutation removing the SQL fence fails this row. **Same tables:** the front doors keep queue, lease, checkpoint, surface-lock and completion state in the `69df2f52` tables (`work_orders`, `agent_runs`, `checkpoints`, `surface_locks`, `work_order_dependencies`, `director_lease`); columns may be added, but no other table holds lease, lock or completion state, and each compatibility-matrix NEW SERVER PRIMITIVE names the table and columns it writes, checked against the OLD DB PRIMITIVE. **Mixed fleet:** an enrolled node is refused a legacy work order, and a legacy run on the unmodified `69df2f52` claim path holding surface X blocks an enrolled claim of a work order owning X, and the reverse. An enrolled run's lease lapses and then a legacy claim runs, with a legacy work order owning the lapsed run's surface at the head of the legacy queue: the legacy claim never fails or reports 'another node won' on that surface, it claims the next eligible legacy work order within one poll, no legacy run holds that surface, and the enrolled run, its work order and its lock are unchanged until the front doors expire or take over the lock. **No plane-conditioned behaviour** (S-10): the verifier's scan of the candidate diff lists no reference that distinguishes the live plane from a disposable one, apart from the production-ref refusal. Each compatibility-matrix regression cites the certified-suite assertion or assertions it ports (file:line at `69df2f52`), and the ported assertion runs through the Node API against the candidate's front doors. The **certified reference suites** (`VERIFICATION_SPEC.md` §3.7) are green; a successor counts only under a ratified change request. A suite that exercises only the frozen raw-SQL path proves legacy non-regression and is never evidence for P-2 on the Node API. | S-10; P-2, P-3 |
| **AC-10 No production authority** | The candidate diff and configuration, plus the Director's read-only observation of the live plane. | Nothing in the diff performs a live migration, Edge deploy, production secret, production deploy or `master` push. The production ref and superusers are refused. The Director re-reads the live plane with `tools/live_catalog_snapshot.mjs` through the observer login (S-11; at candidate stage, before the observer role exists, the documented interim). Before the founder applies the migration, the catalog equals `LIVE_PLANE_CATALOG_SNAPSHOT_PRE_CANDIDATE.json`, except founder-approved changes the Director recorded in the ledger event log (for example the observer role). After it, the catalog equals that snapshot transformed by exactly the catalog difference (additions and removals, after minus before) that the candidate migration produced on a disposable plane provisioned as `69df2f52`, both read with the same tool, plus those recorded changes. Edge Function names, versions, sha256 and updated_at, and the project's secret names (never values), are read by the founder or with a read-only token the founder provides, and compared with the pre-candidate Edge record, committed and cited in the ledger event log: unchanged, except founder-approved changes recorded in the event log, until the founder deploys the certified SHA. Until the record exists, verification runs and receipts proceed, AC-10's Edge clause is recorded PENDING, and only the CERTIFIED ledger entry waits: the Director compares each such candidate's Edge reading against the record before writing its CERTIFIED entry, and never REJECTS for that reason alone. The observer role holds SELECT on every `factory` relation, including those the migration adds, and on `storage.buckets`. The secret scan is clean. The route surface is exactly S-7. | S-7, S-9, S-10, S-11, S-12 |
| **AC-11 Baseline intact** | At every candidate: the candidate migration runs on a copy of the baseline rows (`BASELINE_69df2f52_EVIDENCE_ROWS.json`, loaded unchanged, before the migration, into a disposable plane provisioned as `69df2f52`; the verifier adds no row and fills no column). At acceptance: a read-only re-read of the live plane. | `origin/factory/computer-agnostic-control-plane` = `69df2f52f71fd2bc9415c34fb2be4dab4ee08dd6`; `origin/master` = `55a159172a9bbc9b69cde4d2f832418573a4b0b9` at every candidate. At acceptance, it is that commit or a descendant reached only through the founder-authorized PR(s) recorded in the ledger event log. Each legacy node's plane record shows `head:69df2f52…` clean. No feature or implementation branch is checked out in the live legacy checkout. The evidence-field hashes of every row in `BASELINE_69df2f52_EVIDENCE_MANIFEST.json`, recomputed with `tools/baseline_manifest.mjs`, are **unchanged**: after the candidate migration on the copy, and on the live plane at acceptance. No new two-machine acceptance stamp at `69df2f52` appears. The Director's live-activity comparison (S-15: row hashes new, changed or missing, and counter deltas) shows nothing not explained by a legacy node's own heartbeat or run, a founder action, or a Director-recorded dispatch. The prepared live-migration step is byte-identical to the migration run to completion on the copy, plus the Director-specified wrapper whose manifest recompute runs after the migration's last statement and before commit; its sha256 is in the receipt, and the founder applies exactly that file. It aborts when one evidence field is altered beforehand, and when a mutated migration changes one evidence field. | S-15; P-1, P-8 |
| **AC-12 QA and contract authority** | An implementer or node principal attempts to: (a) alter a receipt; (b) set its own candidate CERTIFIED / CLOSED; (c) create or re-scope a binding WO, or change an AC / S / P row or a canonical document; (d) implement against an unratified change request; (e) create or relax an independence or campaign policy; (f) replace a certified reference suite without a ratified change request. A **holding_admin** and a **founder (tier `founder`)** each attempt (e) through the Admin API, including deleting an S-16 campaign row, and unbinding or rebinding S-16(a). With the **legacy shared credential**, (g): insert a `tenant_admins` row; amend an envelope; relax a policy; write a certification record; publish a release; execute each front door directly; register the reserved capability `factory-enrolled-v1`; self-write `nodes.security_role = release_broker` and then claim a new-model work order; update or delete an enrolled computer's run or checkpoint; delete, extend or re-own an enrolled run's surface lock; delete a dependency row naming a new-model work order; set a verification-required work order to done. | (a) refused or detected by sha256 against the ledger. (b) refused. (c) has no effect: the ledger's document-set hashes expose it, and the candidate tree's copies of the canonical documents must equal the designated Director commit's. (d) and (f) are REJECTED findings. (e) refused for every persona; only stricter changes are accepted. (g) each refused or skipped, with the server rows unchanged. The catalog shows no legacy privilege on authority tables, no legacy EXECUTE on any front door (`has_function_privilege`), and no EXECUTE for PUBLIC on any front door. `pg_auth_members` shows no role reachable from `factory_runner` beyond `69df2f52`, and no API role is a member of it; (g) is also attempted after `SET ROLE` to every role `factory_runner` can reach, and each is refused. **Can the implementer define or alter the contract it is judged against? NO.** | S-10, S-14 |
| **AC-13 Factory V1 accounting (binding)** (II.9) | At final V1 accounting, recorded by the Director. | A cumulative V1 record, with provenance for each part: (1) the **closed two-machine baseline evidence at `69df2f52`**, cited by its manifest (still matching; AC-11) and by ledger 218 (local candidate-repo commit `bf59ea34`), never re-run, recreated or rewritten; (2) the **zero-touch / three-machine release evidence**: AC-1..AC-4 receipts at the certified release SHA; (3) the **remaining Factory V1 gates** G-1..G-6 (contract P-8), each with its state. A record that re-runs, recreates or rewrites (1), or lacks provenance for any part, FAILS. | P-1, P-8 |
| **AC-14 Independence model** (I A.3 §1) | Regression cases. | (a) The same authoring run cannot certify itself. (b) A different hostname alone is insufficient. (c) A different machine fingerprint alone is insufficient. (d) The same hostname can be acceptable when every real requirement passes and the policy permits. (e) A campaign policy requires physical separation without redefining generic independence. (f) No eligible verifier → WAITING, never self-certified. (g) A second run of an authoring identity cannot certify. (h) A certifier without verifier authority in its current envelope is refused. (i) The milestone campaign requires separation for every candidate, whoever the author is. (j) **After a takeover, no member of the authoring set (A or B) can certify.** (k) With policy data relaxed on a copy, the front door still enforces the S-13 floor. (l) A node tries to mint an agent principal, or to act under a principal not bound to its authenticating credential: refused. (m) Two principals on one computer (the second created by the explicit admin action): certification between them follows the policy, and the milestone campaign refuses it (the same enrolled computer record), including when the second principal's runs report a different fingerprint. (n) A computer authors under P1, rotates its credential through the node operation, then attempts to certify that work; the same after a re-pair: the new credential carries P1, and certification is refused. On a computer with P1 and P2, a node-initiated rotate of P2's credential keeps P2. (o) A certification of an earlier candidate (after repair), of another work order, or of a checkpoint other than the completing run's is refused, and it never moves the work order to COMPLETE. (p) One VM enrolled twice through Add Computer with the real runtime: both records report the same machine fingerprint, and a certification between them under the campaign is refused, also after the author's computer later reports a different fingerprint. | S-13, S-16 |
| **AC-15 Eligibility order, preferences, numeric priority** (I A.4 §7, §11) | Scheduling scenarios, including a node that violates two gates at once. | The refusal reason names the **first** failing gate in the founder's order. No ranking input admits an ineligible node. Gate 6 uses the envelope; detection may only restrict. No ranking input, preference or fitness, delays eligible work by more than **30 s** in total. A preference never excludes; only S-16 does. The Home-machine computer is never assigned authoring of Auto-Enrollment product code, including a work order with no declared owned surface, or with one outside the Director-document paths (S-16). Priority orders numerically (2 < 10 < 100). | S-1, S-16; P-6, P-7 |
| **AC-16 No founder poke** (I A.1 §1 `FOUNDER_POKE_NOT_REQUIRED`) | A lease lapses; a verification becomes due; a dependency completes; a candidate fails verification. | Takeover, verification claim, dependent release and return to repair all happen with **no founder keystroke** and no manual role switch. The certified `founder_poke_not_required.mjs` passes, or a ratified successor does. | P-10 |
