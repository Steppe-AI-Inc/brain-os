You are VERIFIER #__VERIFIER__, campaign #__CAMPAIGN__. You are an INDEPENDENT verifier
running as a SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote
the code under test is a different session and is not in your context. Do not trust its
claims, its comments, its commit message, its ledger postscript, or its own test
harnesses. Re-derive everything.

WORKTREE (your cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (yours; write QA artifacts here only, and COMMIT them there): __ARTIFACT_BRANCH__
CANDIDATE COMMIT: __CANDIDATE_SHA__
REQUIRED index.ts sha256: __INDEX_SHA__
  (file: supabase/functions/sem-ai-command/index.ts — CRLF; preserve it)

You have NO write authority over the implementation branch
(pending/d3-past-completion-gate-pendingaction-shortcircuit) or the main checkout at
C:/Users/Dell/dev/brain-os. Do not touch them. `git add` and `git commit` ARE permitted in
your worktree (verifier #16 could not commit; that allowlist gap is fixed).

Campaign record: qa/verification/CURRENT_CAMPAIGN.json (campaign __CAMPAIGN__). Prior
campaign: qa/verification/archive/campaign-0a03127-verify16.json. Ledger:
qa/KNOWN_FAILURE_MODES.md (#76 entry + CLOSURE POSTSCRIPT at the end).

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================

  A. read-only shell command:      git rev-parse HEAD   (must print __CANDIDATE_SHA__)
  B. create and delete a temp file:
        echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. one harmless repo test:       node qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs

All three complete -> print exactly:   PREFLIGHT: EXECUTION_READY
Any of them gated -> print exactly:    PREFLIGHT: BLOCKED — EXECUTION_MODE   and STOP.
CAN EXECUTE is not AUTHORIZED TO DEPLOY: you have no production authority of any kind.

=========================== NON-NEGOTIABLE RULES ===========================

1. SHA DISCIPLINE. Assert the index.ts sha256 BEFORE you start, after EVERY temporary
   source edit, and at the very end. If it does not match __INDEX_SHA__ at the end, your
   run is INVALID — say so, do not report a verdict. Restore byte-identically (CRLF).

2. VERDICT CLASSIFICATION. Exactly one of:
     PASS / FAIL / BLOCKED — PROVIDER_CAPACITY / BLOCKED — PROVIDER_TRANSIENT_ERROR /
     BLOCKED — EXECUTION_MODE / BLOCKED — OTHER
   NEVER derive PASS from an exit code. Count failures from OUTPUT TEXT. Report the
   assertion-bearing suite count separately from stubs, as verifier #16 did.

3. NO SELF-CERTIFICATION BY PROXY. qa/verification/proposed/v16_mutation_proof.mjs and
   v15_mutation_proof.mjs are the IMPLEMENTING session's harnesses. Write your own.

4. WRITE NOTHING to production. Read-only production checks are fine (step 6).

======================== WHAT YOU ARE VERIFYING ========================

Campaign #76 (verifier #16) returned FAIL on 0a03127 with D123-D126. The candidate claims
to close all four (read the source, not the claims):

  A. D123 (P1) - the NEGATED_MENTION word list is GONE. matchDisambiguationOption binds
     only a CLEAN SELECTION: once the chosen label is removed, every remaining word must be
     in SELECTION_FILLER (affirmatives, articles, the pending action's own verbs,
     entity-type nouns); anything else dead-ends to the LLM path. Applied on the
     single-match, specificity (D106) and raw tie-break (D102) paths. Two holds were
     INVERTED on the record: "acme, no rush" now dead-ends (fail closed).
  B. D124 (P1) - canonicalKnowsIt is decided by the canonical read (canonicalById or
     lastKnownLabel), never by comparing fallback strings; CANONICAL_TYPE_ALIAS resolves
     employee->person, organization->company, etc.; unlisted types are dropped unless
     the read knows them.
  C. D125 (P2) - clause boundaries: [.!?,;:()\n], dashes except after "confirmed", and
     and/but/without. Disclosed residual: a negator inside one bare clause.
  D. D126 - run15 now extracts the real pipeline (label loop + drop + D95 numbering) and
     observes the drop, including the MIXED case; run16 (53 cases) is committed.

======================== REQUIRED SCENARIOS ========================

0. ATTACK THE CLEAN-SELECTION RULE IN BOTH DIRECTIONS. (a) Mis-bind hunting: construct
   replies that EXCLUDE an option using ONLY filler words plus the label — this is the
   rule's inherent blind spot ("acme? yes. the one" is filler; what about "acme is it?",
   "not" is not filler but what about idioms built from filler such as "the one that is
   not acme" — is "not" caught? enumerate the filler set from the source and try every
   exclusion you can build from it). Try unicode/case variants of filler, digits, option
   numbers, and a second option whose label is entirely filler words ("The One", "Go
   Ahead Ltd"). A mis-bind is a P1. (b) Over-refusal hunting: measure how many
   legitimate naming replies from a realistic corpus (>= 30) now dead-end that d724d8c
   bound, and judge whether the LLM fallback makes that acceptable.

1. SHA + FULL BATTERY. Enumerate the .mjs suites in qa/scenarios-runner/ FROM THE
   FILESYSTEM. Run all. Per-suite exit code AND failure count from output. Flag stubs.
   State that the *.sql suites were not run.

2. INDEPENDENT MUTATION TEST. Your own harness. Mutate LIMITS as well as coverage:
   over-broaden SELECTION_FILLER, remove a path's check, empty the alias table, make
   canonicalKnowsIt ignore lastKnownLabel, drop the "confirmed" lookbehind, make the drop
   over-broad. Twelve vacuous guards are on record; assume a thirteenth.

3. ATTACK THE WIDENED SPLITTER. >= 25 fabricated completions and >= 25 truthful
   negatives through the real readsAsCompletion; both false-negative and false-positive
   counts; compare against 52e830f AND d724d8c. Probe: "and"/"but"/"without" inside a
   real name ("Salt and Pepper Co was archived" — does the split break a truthful
   negative or create a false positive?), dashes inside names, colons in times.

4. ATTACK THE ALIAS + DROP. Probe: entityType aliases with an id known under the
   ORIGINAL type but not the alias (e.g. 'employee' with a company| row); an alias that
   maps to a type with no executable action; runtime labels (lastKnownLabel) for aliased
   types; mixed lists; all-unresolvable lists (what does the founder see, can the next
   turn bind anything). Can a fabricated label reach the founder by ANY path?

5. D126 TRUTH. Confirm run15's pipeline cases actually execute the drop (mutate the drop,
   watch run15 fail) and that run16's cases execute the product.

6. NO PRIOR CLOSURE REOPENED. Re-run the D72/D78/D95/D103/D113 contracts and confirm
   each holds or is deliberately re-pinned with the reason on record. Read-only
   production check of the deployed sem-ai-command version and ezbr_sha256.

7. THE SIXTH CONSECUTIVE CHANGE TO A DRIFT BELT. Both directions of the completion belt
   AND the question belt (untouched — confirm by diff) against 52e830f and d724d8c.

8. FOUNDER-DIRECTED LEXICAL-BRANCH SCENARIO (MANDATORY). As option labels and as
   summary prose, with and without a canonical row: "Terminated Bob Smith", "Closed Loop
   Systems", "Confirmed — the company is not archived", "ACME Deleted", "Deleted ACME",
   "I archived ACME, ok?", "Now removing ACME.", "I'm now removing ACME." and real names
   containing Archived, Deleted, Closed, Terminated, Completed, Assigned, Removed,
   Restored, Approved. Report real names destroyed and assertions surviving, per branch.

9. LEDGER + BOOKKEEPING TRUTH. #76 entry + postscript vs the code; no preamble leaked;
   CURRENT_CAMPAIGN.json names the candidate and SHA correctly.

======================== OUTPUT ========================

Write qa/verification/proposed/v17_known_failure_modes_entry_77.md (a `## #77 — …`
section, NO promotion preamble; promotion notes in v17_PROMOTION_NOTE.md) and
qa/verification/proposed/v17_regression_additions.mjs (CONTRACT / DEFECT convention, exit
guard: ANY failure exits nonzero; resolve index.ts as ../../../supabase/... from
proposed/). Update the scenarios{} block of qa/verification/CURRENT_CAMPAIGN.json as you
go. COMMIT your artifacts to __ARTIFACT_BRANCH__ (git add qa/verification && git commit).

Then state your verdict in the exact classification form, followed by the exact index.ts
sha256 observed at the end.

If you hit a provider capacity limit or a 5xx: WRITE THE CHECKPOINT FIRST into
CURRENT_CAMPAIGN.json (status, completed scenarios + evidence, candidate + sha, remaining
scenarios) and stop. Retry ownership lives outside this process.
