Verification complete. Artifacts committed to `verify-b386767-campaign105` as `51ac2e2` (local commit; I did not push — the launch brief asked for a commit and I have no write authority on the implementation branch).

---

# VERIFIER #45 — CAMPAIGN #105 FINAL REPORT

**PREFLIGHT: EXECUTION_READY** (HEAD `b386767a…`, scratch write OK, `sem_ai_command_past_completion_claim_regex` 13/0)

## Step 1 — Production bytes, obtained myself

| | |
|---|---|
| `supabase functions list` (I ran it) | `sem-ai-command` **ACTIVE, version 92**, `ezbr_sha256 33255b31728932ea6c4b251194922a594604e603ba70cc601ae569fe3b4fe475`, `updated_at 1788239725518` |
| `git show c9dfab5bd433:…/index.ts` | **`795c20c82301aba1f1731c6b408cc9345e0f86b43a50b0cf5dba6ca78d1f88fc`**, 321,370 bytes — **CONFIRMS** the required value |
| Committed live download (`24bfccf`) | **same git blob** as `c9dfab5bd433` (`795c20c8…`). On-disk copy hashes `49d53882…` only because checkout applied CRLF |

**Provenance stated honestly:** byte-direct at the git-object level on the committed download artifact; version + ezbr digest independently re-observed live by me. `functions download` is refused by this session's classifier — expected per ledger #108, **not** recorded as an open gap. No P0.

## Step 2 — The seven deploy questions

1. **Bytes** — `sem-ai-command/index.ts` and nothing else; `supabase/` tracked file set identical. LF-normalised 4,312 → 5,998 lines, 52 hunks, +1,738/−52. **Identifier delta re-derived: 482 → 687, 205 added, 0 removed, 12 count changes.**
2. **Behaviour preserved** — own corpus, 594 rows (298 truthful negatives with real names, 296 fabrications), with the negator-token-name section (`No Limits Inc`, `Nothing Bundt Cakes`, `Never Summer Industries`, `None The Wiser LLC`, `Nothing But Nets Foundation`, `"Pending review…"`, `"Awaiting approval…"`) in **both** directions:

| quadrant | count |
|---|---|
| **truth regression** | **0** |
| **fabrication regression** | **0** |
| truth improved | 195 |
| fabrication improved | 41 |

   Matcher, **42** disambiguation shapes: 2 divergences, **both candidate improvements** — deployed v92 arms `archiveCompanyIds` on `un-archive ACME Holdings` against a pending *archive*, a live wrong-direction destructive bind the candidate dead-ends.
3. **Reintroduces nothing** — 0 identifiers removed; the run8/D59 `!result.pendingAction` short-circuit is not back.
4. **4476c92** appears only in suite comments; no suite reads its bytes.
5. **#64 D16/D17 14/14, #65 D25 7/7, #65 D27 (row `9dda919c`) 3/3, #66 D40** closed by gate composition; #65 D26's truthful direction 5/5 preserved. All re-derived.
6. **Rollback exact and available** (`c9dfab5bd433`, blob `795c20c8…`, `verify-deployed-bytes.sh` as the post-deploy check). Caveat: CI deploys **all six** functions, so deploy *and* rollback bump five unchanged functions.
7. **Only intended Edge changes.**

## Step 3 / 3b — Adversarial (409 rows): **18 P1, all 18 in one class; 0 fabrication regressions**

Every named attack came back clean: Title-Case negators, `Pending`/`Awaiting` real negations (8/8), post-preposition negators (26/26), reassurance-idiom-then-denial (65/65), R-AUXGAP truthful (8/8), and **both** dash-before-a-CAPITAL names survive with their fabrications caught **lexically**. The dash refusal is correct — I confirm it. **The three "refused" shapes are no longer refused** — all three are caught with their real names surviving; retire that disclosure. `No North Depot was archived.` is destroyed by the candidate **and by v92** — not a regression; the refusal is sound, and only `knownEntityNames` could ever separate it.

**Mutation: 10/10 shipped fixes load-bearing.** Two (idiom strip, R-AUXGAP) read as no-ops on my first probe set — **my probes were deficient, not the fixes**; with engineered rows they re-open 6/10 and 6/6. Recorded, because the next verifier will make the same mistake.

## Step 3c/4 — Counts and suite integrity, measured myself

**Battery 36 suites / 0 non-zero exits / 1,060 assertions / 0 FAIL lines**, exit status read per child process via `spawnSync`, never a pipeline — the earlier "33/0" retraction is warranted. All 15 prior gates at their predicted state, **run15 57/0**. Vacuity proven by mutation: v92 bytes → 20 red; referenced top-level belt const → **8 red incl. 3 ReferenceErrors**; local inside `completionIsNegated` → **0 red**; injected whole-span lookahead → 2 red. **CONTRACT 5's narrowing is empirically honest and hides nothing.** **The eighth vacuity is real and closed** — seed removed, new fuzz finds **211 throws / 2,000**, old alphabet finds **0**. All re-pins honest on my own extraction. All 36 suites cwd-independent. `sha256` re-asserted pristine after every variant.

## Step 5 — Deployability

**Not fit to deploy.** `V45-D1`: the `EXECUTION_IN_PROGRESS` arm destroys an offer explicitly conditioned on the founder's confirmation — v92 keeps all of them; the candidate substitutes a refusal that is itself **false** (archiving *is* a chat capability) and persists it to `work_orders.output`. **117 of 196** on my own generation — ~3× the 39 on record, and covering `I'm about to` / `I am going to` / `let me go ahead and`. I did not close it with a regex; my product view is in the promotion note.

Non-blockers found: **V45-D2** (the entity rescue is the only one of four `Confirmed —` branches lacking the interposed-prefix tolerance; 18 rows), **V45-D6** (`Let me go ahead and archive` escapes both gates, 60/192, v92 misses them too), **V45-N2** (belt runtime is **quadratic** — 657 ms at 33 KB vs v92's 0.085 ms; unmeasured in 45 verifications), **V45-N3** (`detName`'s `/i` folds `[a-z]`; measured inert over 6,600 rows), **V45-N4** (two campaign mutation-proof artifacts cannot reach their input at all).

**Coverage gaps, stated not skipped:** `deno check` **BLOCKED** (no deno; `npx deno` refused) — the "deno 23 == baseline" claim is unverified by me; substituted 226/226 regex literals construct, no inline modifier groups, belt `new Function`-built by 8 suites. No browser, no live AI turn — this is a source differential; every verdict is UNIT VERIFIED / CODE INSPECTED against real deployed bytes, never E2E VERIFIED.

**Synthetic data:** none created. This campaign never wrote to the database or production, so there is no `QA-VERIFY-*` cleanup to do. No migration written, prepared, or pushed. Nothing deployed.

**Artifacts:** `qa/verification/proposed/v45_known_failure_modes_entry_111.md`, `v45_PROMOTION_NOTE.md`, `v45_regression_additions.mjs` (51 pass / 3 fail, exits non-zero, correct from any cwd), plus `qa/verification/scratch/v92/v45/` — commit `51ac2e2`.

**EDGE STATUS = NOT DEPLOYMENT READY. Production stays v92.**

FAIL

`6c5e52b52f1a836670ce043f3a76612a21d27cfc741bff9e6a4bc1e2d410e59d`
