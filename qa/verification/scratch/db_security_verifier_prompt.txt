INDEPENDENT DB/SECURITY VERIFICATION — three PREPARED migrations, three SEPARATE verdicts (campaign db-review-202609020001-2-3).

Repo: C:/Users/Dell/dev/brain-os-bug006 (master worktree). The three files under review, committed as SOURCE ONLY (none applied anywhere; no db push is authorized; you must NOT execute any DDL against production — your production access, if any is granted in your allowlist, is read-only reference):
  1. supabase/migrations/202609020001_chat_channel_state_durable_conversation.sql
  2. supabase/migrations/202609020002_set_person_assignment_clear_manager.sql
  3. supabase/migrations/202609020003_messaging_transport_foundation.sql
Context docs (read, do not trust): qa/verification/proposed/202609020001-channel-state-migration-review.md, docs/MESSAGING-TRANSPORT-ARCHITECTURE.md, post-apply test qa/scenarios-runner/chat_channel_state_rls_personas.sql. The FEATURE-GATED runtime consumer for #1 is in supabase/functions/sem-ai-command/index.ts on branch pending/d3-past-completion-gate-pendingaction-shortcircuit @ C:/Users/Dell/dev/brain-os (grep for chat_channel_state; verify the gate is real: any error -> pre-migration behavior).

A PASS on one migration MUST NOT imply anything about another. Emit one verdict per migration: READY FOR AUTHORIZATION or NOT READY with exact defects.

For 202609020001 verify (at minimum): the EXISTS-under-RLS select policy cannot WIDEN chat_channels visibility in any direction; write policy is the channel WRITE tier spelled out (a reader must not plant pending actions); anon has zero access (grants + policies); authenticated-but-uninvited zero; whole-or-nothing pending action CHECK is complete (no half-written shape passes; NULLs only together); action-type vocabulary constrained; source work-order binding + ON DELETE semantics (reader must treat null source as expired — verify the runtime does); expected-confirmation constrained; expiry present and reader-enforced; version CAS semantics (writer-driven, no trigger interference); cross-org isolation via the channel; focus_stack/resolved_entities can never become an authority source; last_successful_mutation is evidence-derived in the runtime; rollback (drop table) genuinely safe with the feature-gated runtime both before AND after.
For 202609020002: old 11-arg signature dropped (no ambiguous overload possible); all existing callers compatible (positional 11-arg call in 202608280012 line ~351; named-param web caller web/lib/data/people.ts setPersonManager); p_clear_manager explicit; NULL never implicitly clears; clear=true + non-null manager RAISES; search_path='' preserved from 202608280013 (the file documents a caught near-miss — verify the body against the LIVE definition if you have read access, else against 202608280013's text); schema-qualified enum casts preserved; authority check unchanged; cross-org manager assignment impossible through this RPC path (note: the RPC itself doesn't same-company-check the manager — the web action does; decide whether that split is acceptable and record it); anon/public revoked on the new signature; rollback text correct (must restore the 0013 body, not 0011).
For 202609020003: chat_channels stays the canonical layer (bindings reference, never replace); explicit company binding NOT NULL; external identity mapping is founder/admin-only and one-live-mapping-per-identity; unknown external sender acquires zero authority anywhere; anon zero; outbound queue requires channel WRITE tier (viewer cannot speak as the company); disabled binding blocks sends at the queue layer (verify the design doc's claim matches the schema's capability); unique (transport, external_conversation_id) idempotency; duplicate-webhook handling adequacy at schema level; NO secrets columns in ordinary tables; cross-org isolation; rollback order safe.

Also run a same-defect sweep across all three: grant/revoke completeness (202608310004 discipline), search_path on any function, FK delete behaviors, unique constraints vs partial indexes, and anything the three share that could fail together.

Write your record to qa/verification/DB_REVIEW_202609020001-3.json in the master worktree (do NOT touch CURRENT_CAMPAIGN.json — that belongs to the Edge campaign). Checkpoint per-migration; provider-limit rule: checkpoint FIRST, verdicts stay PENDING.
