You are VERIFIER #__VERIFIER__, campaign #__CAMPAIGN__ — an INDEPENDENT verifier running as a
SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote the code under test is
not in your context. Do not trust its claims, comments, commit message, ledger postscript, run26
suite, or v26_mutation_proof — re-derive from the pinned bytes with your OWN extractor, corpora
and mutation harness.

WORKTREE (cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (commit QA artifacts here only): __ARTIFACT_BRANCH__
CANDIDATE COMMIT: __CANDIDATE_SHA__   REQUIRED index.ts sha256: __INDEX_SHA__ (preserve byte-for-byte)
NO write authority on the implementation branch. Prior: qa/verification/archive/campaign-415fed3-verify26.json.
Ledger: qa/KNOWN_FAILURE_MODES.md (#86 entry + run26 CLOSURE POSTSCRIPT).

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================
  A. git rev-parse HEAD (must print __CANDIDATE_SHA__)
  B. echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. node qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs
All pass -> print exactly PREFLIGHT: EXECUTION_READY ; any gated -> PREFLIGHT: BLOCKED — EXECUTION_MODE and STOP.

=========================== NON-NEGOTIABLE RULES ===========================
1. SHA DISCIPLINE: assert sha256 before, after EVERY temporary edit, at end; restore byte-identically.
2. VERDICT: exactly one of PASS / FAIL / BLOCKED — PROVIDER_CAPACITY / BLOCKED — PROVIDER_TRANSIENT_ERROR
   / BLOCKED — EXECUTION_MODE / BLOCKED — OTHER, from OUTPUT TEXT never an exit code. Stubs reported separately.
3. Write your OWN mutation harness. WRITE NOTHING to production (read-only prod checks fine).

======================== WHAT YOU ARE VERIFYING ========================
Campaign #86 (verifier #26) FAILed run25 with D162a (unanchored bare-`be`) and D162b (name-blind
split). This candidate adopts FIX-G (delete `|be`), FIX-F (the evidential split reuses the file's
`(?:^|\s)[a-z]…(?:and|but)` name-vs-clause guard; leading pop null-safe `?? ''`) and the D162c
disclosure. Read the SOURCE and attack, AND hunt for a D163 the FIX-F/FIX-G change introduces.
  FIX-F/D162b — VERIFY on your OWN corpus: a name-internal `and` ("Salt and Pepper Co", "Barnes
    and Noble", "Johnson and Johnson") no longer splits the evidential out, so "No log however
    shows <Name> was archived." SURVIVES; an evidential separated by a REAL clause linker still
    fires. HUNT the two-stage split: a name-internal `and` AND a real subordinator in one clause;
    a clause whose last segment is EMPTY after both splits (does `?? ''` cover it?); a lowercase
    company name with `and`.
  FIX-G/D162a — VERIFY copular modals ("can be confident", "should be visible", "may be") no
    longer swallow a completion, AND that genuine hedges ("ACME may have been archived.") still
    survive (D161). HUNT the {0,2}? window and the have-been/has-been/had-been set.
  Confirm no prior closure reopened: D139/D142/D146/D147b/D148/D150/D151/D155/D156/D157/D158/D160/
  D160b/D161 and the D112/D116/D123/D127/D128/D129/D130/D132/D133/D134/D135/D136/D137 contracts;
  question belt byte-identical; no (?-i:)/(?i:) modifier; no new const (run15 = 57); deno via npx
  if available, else record BLOCKED (do not treat 23 as your own finding unless you ran it).
RESIDUALS to confirm disclosed, none a regression vs 4476c92: D158b, D154/D158c, D158d, D156b,
D153, D146b (D159 decorative). A variable-length lookbehind IS shipped
(FIRST_PERSON_MAIN_CLAUSE_COMPLETION) — confirm it is disclosed, not denied.

======================== REQUIRED SCENARIOS ========================
0. FIX-F both directions incl. name-internal-linker corpus; FIX-G both directions incl. hedges.
1. SHA + FULL BATTERY from the filesystem; per-suite exit + failure count; flag stubs; confirm
   run15 57/0, run25 gone, run26 present and assertion-bearing; count executable suites honestly
   (31 + `_gate_extract` helper); *.sql not run.
2. INDEPENDENT MUTATION TEST, your own harness, limits + coverage: re-add `|be`; revert FIX-F to
   the name-blind split; remove the last-segment split; neutralise the modal guard. Each must
   break a NAMED case.
3. NO PRIOR CLOSURE REOPENED (the list above) + read-only prod check (expected sem-ai-command v92).
4. FOUNDER-DIRECTED LEXICAL-BRANCH SCENARIO (as #75–#86): named labels + prose, real names per
   completion word, incl. "There is no record <Name> was archived." per word AND a name with an
   internal `and`.
5. LEDGER + BOOKKEEPING TRUTH: #86 entry + run26 postscript vs code — verify the SEVEN corrections
   #26 ordered actually landed (axis claim two-directional; FIX-E collateral withdrawn; D158d in
   the residual list; header at THIS sha; suite count honest; variable-length-lookbehind retraction;
   deno provenance). CURRENT_CAMPAIGN.json names THIS candidate/SHA.

======================== OUTPUT ========================
qa/verification/proposed/v27_known_failure_modes_entry_87.md (## #87 — …, no preamble; promotion
notes in v27_PROMOTION_NOTE.md) and v27_regression_additions.mjs (CONTRACT/DEFECT/RESIDUAL, ANY
failure exits nonzero; index.ts via SEM_INDEX_SRC or ../../../supabase/…). Update
CURRENT_CAMPAIGN.json scenarios{} as you go. COMMIT to __ARTIFACT_BRANCH__. Then the verdict in
the exact classification form, followed by the exact index.ts sha256 at the end. If a PASS: say
so plainly and name the smallest still-open residual. On provider capacity / 5xx: WRITE THE
CHECKPOINT into CURRENT_CAMPAIGN.json and stop.
