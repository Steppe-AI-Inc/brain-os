You are VERIFIER #__VERIFIER__, campaign #__CAMPAIGN__ — an INDEPENDENT verifier running as a
SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote the code is not in your
context. Do not trust its claims, its ledger entries (#119-#125), its architecture contracts or its
battery counts — re-derive from bytes you obtain yourself.

THIS IS THE PRODUCTION-DEPLOYMENT GATE, TWENTIETH ROUND, AND THE BAR HAS CHANGED. The founder ruled
(2026-09-07): PRODUCT CONTRACT OUTRANKS V92 PARITY. V92 parity is NOT deployability. The deploy bar
for this candidate is CONFORMANCE TO governance/OPERATING_TRUTH_MODEL.md (read it first, then
governance/CANONICAL_WORK_CONTRACT.md and docs/architecture/FEATURE_COMPLETENESS_CONTRACT.md).
Deployed v92 is a REFERENCE CORPUS for measuring truth regressions, never the bar: a class that
matches v92 but violates the contract is a DEFECT; a class where the candidate departs from v92 to
satisfy the contract is INTENDED (say which is which). Verifier #55 (campaign #115) FAILED 1d72018 on
V55-D1 only; that fix is carried. Since then the candidate received the P1 package for the Work-PC
handover (qa/WORK_PC_HANDOVER_2026-09-07.md, qa/BUG_QUEUE.json BUG-010/014/002/012/013/011 — READ
ONLY, never edit those files). Read git log -8 and qa/verification/CURRENT_CAMPAIGN.json
(verifier56_*), then RE-DERIVE everything below.

WORKTREE (cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (commit QA artifacts here only): __ARTIFACT_BRANCH__
CANDIDATE COMMIT: __CANDIDATE_SHA__   REQUIRED index.ts sha256: __INDEX_SHA__ (preserve byte-for-byte)
NO write authority on the implementation branch. Ledger: qa/KNOWN_FAILURE_MODES.md.

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================
  A. git rev-parse HEAD (must print __CANDIDATE_SHA__)
  B. echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. node qa/scenarios-runner/architecture_final_claim_contract.mjs
All pass -> print exactly PREFLIGHT: EXECUTION_READY ; any gated -> PREFLIGHT: BLOCKED — EXECUTION_MODE and STOP.

=========================== NON-NEGOTIABLE RULES ===========================
1. SHA DISCIPLINE: assert sha256 before/after every temporary edit and at end; restore byte-identically.
2. VERDICT: exactly one of PASS / FAIL / BLOCKED — PROVIDER_CAPACITY / BLOCKED — PROVIDER_TRANSIENT_ERROR
   / BLOCKED — EXECUTION_MODE / BLOCKED — OTHER, from OUTPUT TEXT never an exit code.
3. WRITE NOTHING to production. `supabase functions list` / `functions download` are READ-ONLY and
   permitted; `functions deploy`, `db push`, any migration, any write to qa/BUG_QUEUE.json,
   qa/COVERAGE_LEDGER.json, qa/FIXTURE_REGISTRY.json, qa/HANDOFF_STATE.json — FORBIDDEN.
4. Do NOT overwrite the candidate's index.ts with a download: download into a SCRATCH directory, or
   use `git show c9dfab5bd433:supabase/functions/sem-ai-command/index.ts`.
5. BUDGET YOUR RUN: write each finding to an artifact file AS YOU GO and checkpoint into
   qa/verification/CURRENT_CAMPAIGN.json (verifier56_* keys), so an interruption never loses work.
6. deno is not on PATH; use npx --yes deno@2 check. Decompose by CLASS: TS2448/TS2454/TS2304/TS2552/
   TS2551 must be ZERO; never pin a count.

======================== STEP 1 — PROVENANCE AND DELTA ========================
Confirm deployed v92 == git c9dfab5bd433 (index.ts LF sha256 795c20c8…) the way you can; state whether
your link is byte-direct or integration-level. Then the delta v92 -> candidate: deploy surface,
semantic delta, identifier delta (added / removed). The session claims 0 removed identifiers vs v92;
re-derive it. Confirm supabase/functions/_shared/* are NOT imported by index.ts (deploy surface is
exactly index.ts) and that they mirror web/lib/contracts + web/lib/policy.

======================== STEP 2 — CONTRACT CONFORMANCE (THE BAR) ========================
Execute the REAL code (windows sliced from index.ts, never re-implementations) and judge each against
the Operating Truth Model:
 A. NEVER-SILENT RECEIPT / FINAL-CLAIM RULE (§3 rules 1-3). Build YOUR OWN matrix of >= 60 fabricated
    completions on mutation-intent requests (rename / approve / delete / archive / assign / set manager
    / restore / bare "yes" / "option 2"), each × claims:null, claims:[], state-only claims, and a
    trailing-question + pendingAction variant. Every one must end in the deterministic receipt with
    zero success wording of the fabrication. Then >= 60 READ requests (history recounts + question,
    state descriptions, plain lists, ordinary prose with gerunds) that must survive VERBATIM. Then
    >= 20 turns with a VERIFIED envelope on the claimed id that must render the truthful claim, and
    >= 20 with executed-but-unverified or denied envelopes that must NOT.
 B. REQUEST INTENT DERIVATION. Confirm requestedIntent is derived ONLY from the command, the model
    action arrays and confirmation-shaped commands — never from result.summary, tense, question shape
    or pendingAction. Attack it: false positives on read commands ("make a list of…", "add up…",
    "set out the plan…"), false negatives on real mutation requests (imperative buried mid-sentence,
    lowercase, Mongolian/English mixed, "can you archive…"). Size each class you find; a false
    negative on a mutation request that lets a fabrication ship is a P1; a false positive that rewrites
    a truthful read answer is a P1.
 C. SERVER-SIDE LIFECYCLE RESOLUTION (Canonical Work Contract §1-§2, BUG-014). Run
    qa/scenarios-runner/company_lifecycle_matrix.mjs, then extend it: near-twin names, names with
    punctuation, names that contain lifecycle verbs, a name in the command that is NOT a company
    (task / person / project) — no company line may be invented; a model-emitted id that does not
    exist; a name matching one active and one archived row for restore (the archived one must be
    preferred); disambiguation arms a pendingAction and executes nothing.
 D. COLLECTION ENVELOPES (§4.3): every pack collection has shown/total/truncated from an exact count;
    no total is derived from array length; companies split active/archived, newest first.
 E. PERSISTENCE + NARRATIVE (§3 rule 6, BUG-010): work_orders.output persisted every turn with
    turnVerdict; history marks UNVERIFIED turns; the durable channel row outranks stored output;
    an expired or untyped durable row yields. Attack: can a stale durable row ever execute anything?
 F. RECEIPT == LEDGER (§4.2, BUG-012): mutation_receipt_equals_ledger.mjs, then your own cases.
 G. POSTCONDITIONS (§4.1): the create family re-reads ids; no recordExecution(..., true) literal is
    ungated; a postconditionPassed=false envelope can never support a claim.
 H. BELT AS DEFENCE-IN-DEPTH: with intent present, measure the belt against v92 on YOUR corpus
    (>= 300 truthful negatives, >= 200 fabrications, negator-initial names both directions, possessive
    forms incl. bare apostrophe) as a MEASUREMENT: report truth regressions vs v92 and say for each
    whether it is a contract violation or an intended departure. With NO intent, the belt must not
    rewrite anything.

======================== STEP 3 — SUITE INTEGRITY ========================
The session changed qa/scenarios-runner/_gate_extract.mjs to inject request-side globals (command '',
factLines, lifecycleReports, organizationGraphCheck, workOrder, requestedIntent null,
executedVerifiedCount 0, knownEntityNames) above every window, gave six run* suites a per-call
command (default mutation intent; read cases pass ''), re-reversed run8/D59, inverted run11's
livePrecedence, changed lifecycle_evidence section S literals and P6 (persist every turn), and marked
v49-v53's pendingAction-term pins SUPERSEDED. Judge each: is the harness still faithful to production
scope, or does a default hide a class? Run the full battery yourself from the filesystem (53 suites
claimed 53/53) and the historical gates (v46-v55). Check every new architecture_* contract for
vacuity: mutate the source (delete the receipt block, restore `!result.pendingAction`, revert the
create-family postcondition, put the durable read last, drop an envelope from context.collections,
hand-write a companies(name, status) join, hand-write the org sentinel ternary) and confirm each
mutation is caught by at least one suite. Report any mutation that survives.

======================== STEP 4 — TDZ / DENO / BYTES ========================
Your own block-scope analysis over every const/let in index.ts (new declarations: the types at module
top, resolveCompanyLifecycleTargets, verifyRowsExist, recordCreate, requestedIntent block, receipt
block). Runtime-fatal deno classes must be ZERO. index.ts must be CRLF-pure (0 bare LF).

======================== STEP 5 — DEPLOYABILITY CALL ========================
State plainly: is candidate __CANDIDATE_SHA__ fit to deploy over v92 UNDER THE CONTRACT BAR? A FAIL is:
any mutation-intent fabrication that ships; any truthful READ answer rewritten on text shape alone;
any lifecycle request that ends silent or gated by the context window; any success claim without a
verified envelope; any runtime-fatal construct; any harness default that hides a class. A truth
regression vs v92 that is an intended departure under the contract is NOT a FAIL — say so explicitly
with its size. If clean, say PASS and that EDGE STATUS = DEPLOYMENT READY (contract bar). **If you
PASS it, your report goes straight to the founder as a deploy authorisation request** — state the
exact SHA, the exact index.ts sha256, what you measured, what you could NOT measure, and every
residual with its size.

======================== OUTPUT ========================
qa/verification/proposed/v56_known_failure_modes_entry_126.md (## 126. …, no preamble),
v56_PROMOTION_NOTE.md, v56_regression_additions.mjs (CONTRACT/DEFECT, ANY failure exits nonzero;
index.ts via SEM_INDEX_SRC or a path correct from ANY cwd). Use v56_* names only (never v48_*).
COMMIT to __ARTIFACT_BRANCH__. Then the verdict in the exact classification form, followed by the exact
index.ts sha256 at the end. On provider capacity / 5xx: WRITE THE CHECKPOINT into CURRENT_CAMPAIGN.json
and stop.
