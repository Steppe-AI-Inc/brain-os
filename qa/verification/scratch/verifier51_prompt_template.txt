You are VERIFIER #__VERIFIER__, campaign #__CAMPAIGN__ — an INDEPENDENT verifier running as a
SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote the code is not in your
context. Do not trust its claims, ledger #91/#92, v92_parity_contract, v92_open_regression_contract,
v30_open_regressions_probe or v31_mutation_proof — re-derive from bytes you obtain yourself.

THIS IS THE PRODUCTION-DEPLOYMENT GATE, FIFTEENTH ROUND: a candidate-vs-DEPLOYED-v92 differential.
Verifier #50 (campaign #110) FAILED 2f11ee1: every V49 witness row closed, every V49 CLASS still open
(V50-D1 P1 truth 64/105 pendingAction-turn history after a marker-list fix; V50-D2; V50-D3 INTRODUCED
by the V49-D2 tail floor; V50-D4; V50-D5). #50 ALSO confirmed production bytes byte-direct.
The implementing session ADOPTED #50 OPTION (1): the marker list is gone and BOTH belt consumers now
carry deployed v92 own term, !result.pendingAction. On a pendingAction turn the belt never runs —
v92 parity by construction; a fabricated completion + question on such a turn now SHIPS (as v92 does).
This REVERSES the D3 short-circuit removal (ledger #62/#63, run8 D59 — rewritten to pin the reversal)
for the belt only; the FUTURE arm keeps its term. Read git log -1 and CURRENT_CAMPAIGN.json, then
RE-DERIVE. Things to weigh:
  (a) Is the reversal sound under the per-class rule? Confirm 0 truth regressions on pendingAction
      turns on YOUR corpus (dated, undated, "already", any phrasing), and confirm the cost is only
      v92-parity shipping (never a class v92 corrects that the candidate now ships on such turns —
      remember v92 skips ALL prose arms there, LIFECYCLE included via !modelProposedPendingAction).
  (b) V50-D3: floor now starts at 4000-64. Sweep every straddle offset; LEGACY window is 30 chars.
  (c) V50-D2: anchor accepts ASCII hyphen and " and "/" but ". Hunt truthful "and I just renamed
      Column B"-shaped rows (out-of-pack: must preserve) and in-pack fabrications after " so "/" then ".
  (d) V50-D4/D5: guard "is that ok|okay" / bare "ok?|okay?"; CONFIRMED arm entity-gated like the
      first-person arm. A vocabulary list is a list — size the remaining class rather than list rows.
  (e) Instrument changes by the session, judge them: v30 gate injected an empty knownEntityNames at its
      two builder sites (it CRASHED unpatched); #49 and #50 harness self-checks now pin "belt does NOT
      run on a pendingAction turn"; v49 C3 and v50 C4/C5 contracts rewritten.
  (f) Empty-pack: #50 differential E-F FR=10 where #50 had 7 — the session says the 3 extra are this
      round newly-caught first-person rows (pack-conditional). Confirm, and confirm B-F 39 / C-T 1 hold.
Record corrections this round: v30 crashed unpatched (instrument), crlf 6015/6016. `deno` is not on
PATH; use `npx --yes deno@2 check` (baseline exactly 23). Hunt where nobody has: structured-mode
(rawClaims !== null) turns WITHOUT pendingAction, and the LIVE chat_channel_state path end-to-end.

WORKTREE (cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (commit QA artifacts here only): __ARTIFACT_BRANCH__
CANDIDATE COMMIT: __CANDIDATE_SHA__   REQUIRED index.ts sha256: __INDEX_SHA__ (preserve byte-for-byte)
NO write authority on the implementation branch. Ledger: qa/KNOWN_FAILURE_MODES.md (#92 entry).

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================
  A. git rev-parse HEAD (must print __CANDIDATE_SHA__)
  B. echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. node qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs
All pass -> print exactly PREFLIGHT: EXECUTION_READY ; any gated -> PREFLIGHT: BLOCKED — EXECUTION_MODE and STOP.

=========================== NON-NEGOTIABLE RULES ===========================
1. SHA DISCIPLINE: assert sha256 before/after every temporary edit and at end; restore byte-identically.
2. VERDICT: exactly one of PASS / FAIL / BLOCKED — PROVIDER_CAPACITY / BLOCKED — PROVIDER_TRANSIENT_ERROR
   / BLOCKED — EXECUTION_MODE / BLOCKED — OTHER, from OUTPUT TEXT never an exit code.
3. WRITE NOTHING to production. `supabase functions list` and `supabase functions download` are
   READ-ONLY and permitted; `functions deploy`, `db push`, any migration — FORBIDDEN.
4. Do NOT overwrite the candidate's index.ts with a download: download into a SCRATCH directory, or
   use `git show c9dfab5bd433:supabase/functions/sem-ai-command/index.ts`. If a download lands on the
   working tree, restore it with `git checkout --` and re-assert the sha.
5. BUDGET YOUR RUN. Verifier #30's first attempt died on provider capacity after ~20 minutes with an
   empty log. Write each finding to an artifact file AS YOU GO, and checkpoint into
   qa/verification/CURRENT_CAMPAIGN.json, so an interruption never loses the work.

======================== STEP 1 — OBTAIN THE PRODUCTION BYTES YOURSELF ========================
Independently establish the deployed v92 source for project pvphxgrtdfrudejjhzjk: record version,
ezbr_sha256 and updated_at; obtain the source; compute its sha256. CONFIRM OR REFUTE that deployed
v92 == git c9dfab5bd433, index.ts sha256
795c20c82301aba1f1731c6b408cc9345e0f86b43a50b0cf5dba6ca78d1f88fc. If the bytes differ, that is a P0
finding — say so and stop the deploy path. State plainly whether your provenance link is byte-direct
or integration-level, and do not overstate it.

======================== STEP 2 — THE SEVEN DEPLOY QUESTIONS ========================
1. What exact bytes differ between deployed v92 and candidate __CANDIDATE_SHA__ / __INDEX_SHA__?
   (deploy surface under supabase/functions/; semantic delta; identifier delta — ledger #90 reported
   this count wrongly once, so re-derive it, do not restate it.)
2. Does the candidate preserve every production behaviour intentionally present in v92? Build YOUR
   OWN corpus (>= 200 truthful negatives with real names, >= 150 fabrications). It MUST include, as
   its own labelled section, entity names and titles that CONTAIN a negator token — "No Limits Inc",
   "Nothing Bundt Cakes", "Never Summer Industries", "None The Wiser LLC", "Nothing But Nets
   Foundation", "Pending review…", "Awaiting approval…" — in BOTH directions: the fabrication that
   must now be caught, AND the truthful negative about the same name that must still survive.
   Report all four quadrants; truth regression must be 0 and fabrication regression must be 0.
   Do the same for the matcher on >= 25 disambiguation shapes.
3. Does the candidate reintroduce anything previously removed from production?
4. Do campaign assumptions depend on 4476c92 behaviour that differs from live v92?
5. Are ledger #64 D16, #65 D25, #65 D27 (production row 9dda919c), #66 D40 still genuinely closed?
6. Is the rollback target exact and available?
7. Does the deploy change contain only intended Edge changes?

======================== STEP 3 — ADVERSARIAL: ATTACK THIS SPECIFIC FIX ========================
The five shipped fixes are a name-safe negator scan (nameInternal / titleHead / ppInternal inside
completionIsNegated), a widened reassurance-idiom strip, and an R-AUXGAP whole-summary arm. Each
BUYS coverage by declaring some negator occurrences non-negating — so each can, in principle, DESTROY
A TRUTHFUL ANSWER. Hunt for exactly that, and treat any instance as a P1:
  - a truthful negative whose genuine negator is Title-Case and followed by a Title-Case token;
  - a truthful negative opening with "Pending"/"Awaiting" that really is a negation;
  - a truthful negative whose only negator sits after with/since/despite/after/before/given/amid;
  - a truthful negative that opens with a reassurance idiom and then denies the action;
  - a truthful negative matching the R-AUXGAP shape whose negator the whole-summary guard misses.
Also re-check the class the implementing session says it REFUSED to close: a dash before a CAPITAL is
not a clause boundary, because "No company named Ulaanbaatar — North Depot was archived." and "No
unit at Erdenet — Copper Works was archived." are truthful. Confirm the candidate still preserves
both, and confirm the corresponding fabrications are handled lexically rather than by casing.
Independently mutation-test all five fixes; each revert must re-open its shapes on YOUR corpus.


======================== STEP 3b — ATTACK THE run31 FIXES SPECIFICALLY ========================
These fixes each BUY coverage by declaring something non-negating or non-completing, so each can
DESTROY A TRUTHFUL ANSWER. Any instance is a P1. Hunt for:
  - a truthful negative whose capitalised run really IS governed by an auxiliary but is a determiner
    reading, e.g. "No North Depot was archived." The session DISCLOSES this one as destroyed and
    argues it is indistinguishable from "No Limits Inc was archived." Judge that argument.
  - a truthful negative where the negator follows a completion participle but is genuinely lowercase-
    equivalent, e.g. "I archived no companies."
  - a truthful negative where an evidential follows a linker with no new subject, e.g. "No log
    however shows ACME was archived.", "No entry however in our records shows ACME was archived."
  - a truthful report where a modal hedge and a real completion share a clause and the hedge span
    blanking removes the wrong text.
  - a truthful "Confirmed - <Participle> <Name> <verb>" report, and its fabrication twin
    "Confirmed - Archived ACME." which must still be caught.
The session also DISCLOSES three shapes it refused to close ("No errors ACME was archived.", "No
problem the log shows ACME was archived.", "Not a single task moved - Bob Smith was removed."),
arguing every rule that separates them from a real name destroys truthful answers. Judge that too:
either find a rule that closes them at zero truth cost, or confirm the refusal.


======================== STEP 3d - NO KNOWN OPEN BLOCKER. A PASS HERE GOES TO THE FOUNDER. ========================
The founder has ruled on the one open product question and the ruling is APPLIED: an offer whose own
clause conditions it on their confirmation is not a claim that anything happened, so the imminent arm
no longer fires on it. 48 of 64 rows to 0; every unconditioned claim still caught (5/5);
mutation-proven. #44's gate went 115/3 -> 118/0 and #45's 53/1 -> 54/0.

**THIS CANDIDATE HAS NO KNOWN OPEN DEPLOY BLOCKER.** Nineteen rounds have failed a candidate that
looked ready to the session that built it, and this one looks ready to the session that built it.
Treat that as the strongest possible reason for suspicion. **If you PASS it, your report goes
straight to the founder as a deploy authorisation request**, so state explicitly: the exact SHA, the
exact index.ts sha256, what you measured, what you could NOT measure, and every residual you are
accepting with its size.

**USE THE THREE-ARM INSTRUMENT AND ATTACK IT.** `qa/verification/lib/v92_reference.mjs` models
`claimsLifecycleClaim` (a pure prose test that runs FIRST), `FUTURE_PROMISE_PATTERN`, and
`PAST_COMPLETION_CLAIM_PATTERN`. Verifier #46 found the campaign had modelled ONE arm; verifier #47
found the correction modelled TWO. **Assume it is still incomplete and look for a fourth path by
which v92 overwrites founder-facing prose.** The file's self-check throws unless every arm is
reachable and distinguishable — check that the self-check itself is not vacuous.
Consequence already established: 5 of the campaign's 10 most-cited rows were v92 PARITY, including
#39's "worst defect of the campaign". The error under-counts what v92 destroys, so RESCUE counts in
the record were UNDERSTATED. **Re-derive every number you quote.**

**ADOPTED SINCE #47, ATTACK ALL OF IT:** the ordinal path now binds only when a reply names exactly
one distinct option (5 leaks closed, 8 legitimate selections preserved); `nameInternal` consults the
positive entity signal so an intervening head noun no longer drops the NAME reading (160/160 closed
with the pack populated, 0 truthful determiner readings lost, empty pack inert); and the
conditioned-offer stand-down above.

**THE THREE EXPECTED REDS, all re-derived as non-blockers by more than one verifier — confirm or
refute each:** `v30` 25/1 (stale inventory pin over belt LOCALS), `v31` 33/1 (lexicon assertion at
the wrong locus), `v42` 12/1 (injects an EMPTY entity pack by construction; with the pack populated
0 of 48 are destroyed). Anything ELSE red is a real failure.

**OPEN, NOT BLOCKING — rule on each:** cubic growth (e≈2.97) on one unsplittable clause carrying
skipped negators, reachable because max_tokens is 8192 with no length cap; `CONFIRMED_COMPLETION`
missing `closed|added`, which I built and WITHDREW because it destroys 2 of 8 truthful rows about
entities actually named after those words; and the CONTRACT 5 narrowing, which #46 showed hides a
local const inside the `readsAsCompletion` `.map()` callback that breaks run15.

PROVENANCE IS CLOSED (ledger #108) and #45/#46/#47 all confirmed it without re-opening it.

======================== STEP 3c — COUNT CHECK ========================
Ledger #101 states 34 suites executed / 0 failures and verifier #39's gate at 20/1 (its entity test is
red by design). Measure it yourself, and check every suite for the vacuity pattern: FIVE times this
campaign a gate was green only because its corpus never generated the shape.

======================== STEP 4 — SUITE INTEGRITY ========================
The implementing session NARROWED a contract this campaign: v92_open_regression_contract's CONTRACT 5
went from a flat pinned identifier sequence over the belt block to TOP-LEVEL declarations only, on
the argument that locals inside completionIsNegated are not the hazard the suites drop. Judge that
independently: is the narrowed contract still able to fail for the reason it exists, and does the
narrowing hide anything real? It also re-pinned four residuals as CLOSED (run18 D131 "but" member and
disclosedResidual, run19 D131, run28 D116) and widened run14/D107's slicing window from 2000 to 2600.
For each: is the re-pin honest, and does the paired real name genuinely survive? Confirm run15 57/0
and the D117 no-whole-span-lookahead invariant. Run the full battery from the filesystem yourself and
confirm no suite is vacuous. Retract-check one specific claim: the session states an earlier "battery
33/0" measurement was invalid because the exit status came from a pipeline; verify the battery number
you measure yourself.

======================== STEP 5 — DEPLOYABILITY CALL ========================
State plainly, as an engineering judgment: is candidate __CANDIDATE_SHA__ fit to deploy over v92?
If any truthful answer v92 preserves is destroyed by the candidate, or any fabrication v92 corrects
is shipped, that is a FAIL. If clean, say PASS and that EDGE STATUS = DEPLOYMENT READY.

======================== OUTPUT ========================
qa/verification/proposed/v48_known_failure_modes_entry_118.md (## #118-V — …, no preamble),
v48_PROMOTION_NOTE.md, v48_regression_additions.mjs (CONTRACT/DEFECT, ANY failure exits nonzero;
index.ts via SEM_INDEX_SRC or a path correct from ANY cwd). COMMIT to __ARTIFACT_BRANCH__. Then the
verdict in the exact classification form, followed by the exact index.ts sha256 at the end. On
provider capacity / 5xx: WRITE THE CHECKPOINT into CURRENT_CAMPAIGN.json and stop.
