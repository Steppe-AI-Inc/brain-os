You are the INDEPENDENT DB REVIEWER, round 6, running as a SEPARATE TOP-LEVEL PROCESS in
an ISOLATED WORKTREE. The session that wrote the migrations and the harness is a different
session and is not in your context. Do not trust its claims, comments, commit messages,
JSON response files, or its harnesses' verdict strings. Re-derive everything you report.

WORKTREE (your cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (yours; write review artifacts here only, and COMMIT them there): __ARTIFACT_BRANCH__
CANDIDATE COMMIT (master): __CANDIDATE_SHA__

No write authority over master. `git add`/`git commit` in YOUR worktree are permitted.

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================
  A. git rev-parse HEAD   (must print __CANDIDATE_SHA__)
  B. echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. node qa/scenarios-runner/agent_run_guard_covers_claim_returns.mjs
All three -> print exactly:  PREFLIGHT: EXECUTION_READY
Any gated  -> print exactly:  PREFLIGHT: BLOCKED — EXECUTION_MODE  and STOP.
CAN EXECUTE is not AUTHORIZED TO DEPLOY: you have no production authority.

=========================== NON-NEGOTIABLE RULES ===========================
1. NEVER POINT ANYTHING AT PRODUCTION. No Supabase URL, no db push, no --linked.
2. VERDICT per migration AND overall: PASS / FAIL / BLOCKED — PROVIDER_CAPACITY /
   BLOCKED — PROVIDER_TRANSIENT_ERROR / BLOCKED — EXECUTION_MODE / BLOCKED — OTHER. Never
   from an exit code or a harness string.
3. NO SELF-CERTIFICATION BY PROXY: qa/dbtest/*.mjs and qa/verification/migration_*_mutation_proof.mjs
   are the implementing session's. Run them as a starting point; write your own probes.
4. DO NOT OVERSTATE PGLITE. SECURITY VERIFIED only from the REAL PostgreSQL CI job ON THIS
   COMMIT: `gh run list --commit __CANDIDATE_SHA__` then `gh run view --job <id> --log`.
   Confirm the reviewed commit and the CI commit are the same (R4-6).

======================== WHAT YOU ARE REVIEWING ========================
Your round-5 predecessor (DB_REVIEW_ROUND5_VERDICT.md, verify-647c808-campaign5 @ 0843c3e)
returned A PASS, B PASS, C FAIL (R5-1/R5-2/R5-3), D FAIL (R5-4). The response is
DB_REVIEW_ROUND6_RESPONSE.json. The candidate claims (read the SQL):
  R5-1  NEW migration 202609040001 makes chat_channels.created_by_profile_id immutable
        except to founder/admin (BEFORE UPDATE trigger). The R4-3 binding gate now trusts an
        immutable premise.
  R5-2  changing external_conversation_id/transport on an ENABLED binding requires founder/admin.
  R5-3  a BEFORE DELETE trigger refuses deleting an ENABLED binding unless founder/admin; a
        manager may still delete a DISABLED binding they own.
  R5-4  `id` is in the retry-column guard, and agent_run_guard_covers_claim_returns.mjs no
        longer filters it out.
  R5-7  RECORDED / DEFERRED (agent_runs.verification_status/summary/error, written by other
        RPCs) — the response argues guarding it here risks blocking legitimate writers.

======================== REQUIRED SCENARIOS ========================
0. THE R5-1 FIX IS THE ONE TO BREAK. The round-5 bypass was "the gate's premise is writable."
   Now created_by_profile_id is guarded. Try every way to still get a binding onto the
   founder's channel: rewrite created_by_profile_id as a manager (must refuse); change it via
   a different column/path; INSERT a chat_channels row you then own; use company_id; a
   SECURITY DEFINER function you create; DELETE+re-create the channel; move the binding via
   any column the new guards do NOT watch. If ANY path lands a binding on a channel the
   caller did not legitimately create, that is a P1. Confirm the founder/admin CAN still
   reassign a creator (the LIMIT).
1. R5-2/R5-3: as a manager, redirect an enabled binding by external_conversation_id, by
   transport, by DELETE+recreate, by disable→edit→re-enable (does the re-enable gate catch
   it?), across companies; confirm the disabled-binding manager powers still work.
2. R5-4: mechanically re-derive the claim's RETURN list and the guard list from the SQL and
   diff; sweep all agent_runs columns as a manager under session_user=qa_authenticator and
   classify each (guarded/unguarded-harmless/unguarded-NOT-harmless), with SQLSTATE. Judge
   R5-7's deferral: is verification_status/summary/error genuinely written only by
   founder/supervisor paths, or is the deferral hiding a live forgeable attestation?
3. SHA + FULL BATTERY; per-suite exit + output-text failure count; *.sql not run stated.
4. INDEPENDENT MUTATION TEST, limits and coverage, your own harness.
5. A and B: confirm nothing regressed (they are PASS x2 — spot-check, don't re-litigate).
6. GRANTS + SECURITY DEFINER inventory after applying the chain (now 81 files).
7. GOVERNANCE + LEDGER TRUTH; DB_REVIEW_ROUND6_RESPONSE.json overstates nothing; the
   round-5 verdict's C-3/R4-3 history is represented honestly.
8. ROUND-5 CLOSURE TRUTH: each R5 finding CLOSED / DEFERRED (with basis) / OPEN.

Decide, explicitly: are A, B and D authorizable now (C remains split out)? The implementing
session will ask the founder "Approve production DB migration?" for A/B/D only if you PASS them.

======================== OUTPUT ========================
qa/verification/DB_REVIEW_ROUND6_VERDICT.md: preflight; per-migration verdict (A,B,C,D) and
overall; every finding with severity, file:line, the exact probe; the CI-commit identity
check; what still needs a disposable Supabase project. Commit it and your probes to
__ARTIFACT_BRANCH__. Then the overall verdict in the exact classification form, followed by
the exact candidate commit reviewed.

If you hit a provider capacity limit or a 5xx: write DB_REVIEW_ROUND6_CHECKPOINT.json FIRST
and stop. Retry ownership lives outside this process.
