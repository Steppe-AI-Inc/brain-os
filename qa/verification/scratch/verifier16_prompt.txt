You are VERIFIER #16, campaign #76. You are an INDEPENDENT verifier
running as a SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote
the code under test is a different session and is not in your context. Do not trust its
claims, its comments, its commit message, its ledger postscript, or its own test
harnesses. Re-derive everything.

WORKTREE (your cwd, read-only candidate): /c/Users/Dell/dev/brain-os-verify-0a03127
ARTIFACT BRANCH (yours; write QA artifacts here only): verify-0a03127-campaign76
CANDIDATE COMMIT: 0a031277486041da72002a88edc3f7e86ddb473d
REQUIRED index.ts sha256: 0c3616b4e82b53f18e0b597aa0fe935b4c0bed1dcae86fbc9d4c59b91812fc26
  (file: supabase/functions/sem-ai-command/index.ts)

You have NO write authority over the implementation branch
(pending/d3-past-completion-gate-pendingaction-shortcircuit) or the main checkout at
C:/Users/Dell/dev/brain-os. Do not touch them. Everything you write goes under
qa/verification/ in YOUR worktree.

Campaign record: qa/verification/CURRENT_CAMPAIGN.json (campaign 76). Prior
campaign: qa/verification/archive/campaign-d724d8c-verify15.json. Ledger:
qa/KNOWN_FAILURE_MODES.md (#75 entry + CLOSURE POSTSCRIPT at the end).

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================

Before anything else, prove you can execute — a detached process that silently waits on
an invisible approval prompt is the failure this preflight exists to catch:

  A. run ONE read-only shell command:      git rev-parse HEAD   (must print 0a031277486041da72002a88edc3f7e86ddb473d)
  B. create and delete ONE temp file in your worktree:
        echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. run ONE harmless repo test:           node qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs

If all three complete, print exactly:   PREFLIGHT: EXECUTION_READY
If any of them cannot run because you are gated (plan mode, approval prompt, permission
denied), print exactly:   PREFLIGHT: BLOCKED — EXECUTION_MODE   and STOP — do not attempt
the campaign. Do not confuse CAN EXECUTE with AUTHORIZED TO DEPLOY: this preflight
proves nothing about production authority, and you have none.

=========================== NON-NEGOTIABLE RULES ===========================

1. SHA DISCIPLINE. Assert the index.ts sha256 BEFORE you start, after EVERY temporary
   source edit you make, and at the very end. If it does not match 0c3616b4e82b53f18e0b597aa0fe935b4c0bed1dcae86fbc9d4c59b91812fc26 at the
   end, your run is INVALID — say so, do not report a verdict. Restore every temporary
   edit byte-identically and prove it with a hash. The file is CRLF; preserve it.

2. VERDICT CLASSIFICATION. Your final verdict must be exactly one of:
     PASS
     FAIL
     BLOCKED — PROVIDER_CAPACITY
     BLOCKED — PROVIDER_TRANSIENT_ERROR
     BLOCKED — EXECUTION_MODE
     BLOCKED — OTHER
   NEVER derive PASS from a process exit code. A suite that prints failures and exits 0
   is a FAIL, not a PASS. exit 0 together with provider-capacity / session-limit text is
   BLOCKED — PROVIDER_CAPACITY, never PASS. Count failures from OUTPUT TEXT.

3. NO SELF-CERTIFICATION BY PROXY. qa/verification/proposed/v15_mutation_proof.mjs is
   the IMPLEMENTING session's harness. Running it and reporting its result is NOT
   independent verification. Write your own mutation harness from scratch.

4. WRITE NOTHING to production. No DB writes, no function deploys. Read-only checks
   against production are fine (see step 6).

======================== WHAT YOU ARE VERIFYING ========================

Campaign #75 (verifier #15) returned FAIL on d724d8c with D116-D122. The candidate claims
to close all seven with these structural changes (read the source, not the claims):

  A. D116 (P1) - matchDisambiguationOption now dead-ends, BEFORE its single-match return,
     on any option whose mention sits in a clause carrying a negator/exclusion word
     (NEGATED_MENTION). The negation is never interpreted. Two pinned LIMITS: the test is
     clause-scoped, and the option's own label is removed before the negator test.
  B. D117+D118 - the whole-summary negation lookahead is GONE from CONFIRMED_COMPLETION;
     NEGATED_CLAUSE (the D112 negator list) is applied ONCE, PER CLAUSE, inside
     readsAsCompletion for every arm (clauses split on . ! ? , ;). Disclosed residual: a
     fabrication and a negator in the SAME clause still disarms that clause.
  C. D119 - the absent-branch LEXICAL FALLBACK IS GONE. An option whose id the canonical
     read cannot name is DROPPED before the founder sees it (it could never execute —
     run13/D103); if nothing is left the pending action degrades to an open question.
     run8/D72b is RETIRED on the record. Six committed suites were re-pinned
     (marked "run15/D119 RE-PIN"). This is the product decision verifier #15 put to the
     founder; judge whether it was implemented as described and whether it is SAFE.
  D. D120 - closed by C's mechanism, NOT by adding EXECUTION_IN_PROGRESS to the label
     channel (the founder's rule: no new regex chain where canonical identity exists).
     Confirm no progressive assertion can reach the founder as a label by ANY path.
  E. D121 - run15_defect_closure_contract.mjs (48 cases) is committed.
  F. D122 - issue5_confirmation_action_type_binding.mjs now extracts and executes the
     REAL resolveClarificationField; the call-site comment cites the drift guard.

======================== REQUIRED SCENARIOS ========================

0. ATTACK THE D116 GUARD IN BOTH DIRECTIONS. (a) Mis-bind hunting: find ANY reply that
   excludes an option yet binds it — negation phrased without a listed negator, negation
   split across clause boundaries, negators inside quotes, unicode dashes, "acme? no,
   the holdings one", double negatives, negation of a DIFFERENT option in the same
   clause ("not beta, acme"). A mis-bind is a P1. (b) Over-refusal hunting: legitimate
   replies that now dead-end — real names containing negator words ("No Limits Inc",
   "Not Just Bagels", "Except Studios"), replies with a negator in another clause. Count
   both directions. A dead-end is not a P1 but a REGRESSION if a previously-binding
   legitimate reply now dead-ends; measure against d724d8c for that.

1. SHA + FULL BATTERY. Enumerate the .mjs suites in qa/scenarios-runner/ FROM THE
   FILESYSTEM. Run all of them. Report per-suite exit code AND failure count parsed from
   output. Flag any suite that asserts nothing. State explicitly that the *.sql suites
   were not run and that DB/RLS/lifecycle truth is out of scope for this run.

2. INDEPENDENT MUTATION TEST. Your own harness, from scratch. For each of the five
   structural changes (A-D, F), mutate the REAL source and confirm a committed case
   fails. MUTATE LIMITS AS WELL AS COVERAGE: over-broaden NEGATED_MENTION, drop the
   clause split, make NEGATED_CLAUSE match everything, make the D119 drop over-broad,
   restore the lexical fallback. This project has shipped eleven vacuous guards; assume
   a twelfth is present.

3. ATTACK THE CLAUSE-SCOPED NEGATION. Build >= 25 fabricated completion summaries and
   >= 25 truthful negatives/non-completions and run them through the REAL
   readsAsCompletion extracted from index.ts. Report false negatives AND false positives.
   Specifically probe the disclosed residual (fabrication + negator in one clause) and
   quantify it; probe clause boundaries the splitter does not know (em dash, colon,
   "and", newline); probe negators that are part of a name or a noun phrase.

4. ATTACK THE D119 DROP. Probe: an option list where ALL ids are unresolvable (what does
   the founder see? can the next turn bind anything?); a MIX of resolvable and
   unresolvable; an id resolvable only via lastKnownLabel maps rather than contextPack;
   two resolvable options with identical canonical names; entity types outside the
   canonical buckets ('record', 'work_order'); a label that agrees with the canonical
   name but differs in presentation (quotes, case). Can a fabricated or model-authored
   label reach the founder by ANY path now? Can dropping options strand a legitimate
   flow that d724d8c handled? Measure the six re-pinned suites: do the new pins still
   observe the properties they used to (numbering, flag quietness, prompt survival)?

5. D122 TRUTH. Confirm issue5_confirmation_action_type_binding.mjs actually executes the
   extracted product function (mutate the product and watch it fail) and that the
   call-site comment is accurate.

6. NO PRIOR CLOSURE REOPENED. Re-run the D72-class contracts in run8/run10/run11/run12/
   run13/run14 and confirm each still holds, noting exactly which were RE-PINNED under
   D119 and whether each re-pin weakened, preserved, or strengthened the property.
   Separately, do a read-only production check of the deployed sem-ai-command version
   and its ezbr_sha256 and record it, so the gap between candidate and production stays
   visible.

7. THE FIFTH CONSECUTIVE CHANGE TO A DRIFT BELT. Campaigns #72-#75 each closed one
   direction by reopening the other. Measure the candidate against d724d8c on BOTH
   directions of the completion belt AND both directions of the question belt (which
   this candidate did not touch — confirm that with a diff).

8. FOUNDER-DIRECTED LEXICAL-BRANCH SCENARIO (MANDATORY). Attack, both as OPTION LABELS
   and as SUMMARY PROSE, with and without a canonical row behind the id:
     "Terminated Bob Smith", "Closed Loop Systems", "Confirmed — the company is not
     archived", "ACME Deleted", "Deleted ACME", "I archived ACME, ok?",
     "Now removing ACME.", "I'm now removing ACME."
   and adversarial REAL names containing each of: Archived, Deleted, Closed, Terminated,
   Completed, Assigned, Removed, Restored, Approved. Report, as separate numbers: real
   names destroyed and execution assertions surviving, in the branch where a canonical
   row EXISTS and in the branch where it does NOT (where "surviving" now includes "the
   option was dropped but the assertion reached the summary by another route").
   EXPLICITLY JUDGE whether the D119 remedy (drop, do not gate lexically) is acceptable.

9. LEDGER + BOOKKEEPING TRUTH. Verify the #75 entry and CLOSURE POSTSCRIPT describe what
   the code actually does, that no PROPOSED-file preamble leaked in, and that
   CURRENT_CAMPAIGN.json accurately names the candidate and SHA.

======================== OUTPUT ========================

Write your findings to qa/verification/proposed/v16_known_failure_modes_entry_76.md
(as a `## #76 — …` section, NO promotion preamble inside it — promotion notes go in a
SEPARATE file v16_PROMOTION_NOTE.md) and any new executable cases to
qa/verification/proposed/v16_regression_additions.mjs using the same CONTRACT / DEFECT
convention and the same exit guard as run15_defect_closure_contract.mjs (ANY failure
exits nonzero). Update the scenarios{} block of qa/verification/CURRENT_CAMPAIGN.json as
you go so the campaign is resumable if you are interrupted. Commit your artifacts to
YOUR artifact branch (verify-0a03127-campaign76) only.

Then state your verdict in the exact classification form from rule 2, followed by the
exact index.ts sha256 you observed at the end.

================== IF YOU HIT A PROVIDER CAPACITY LIMIT OR A PROVIDER 5xx ==================

WRITE THE CHECKPOINT FIRST. Into qa/verification/CURRENT_CAMPAIGN.json record: status
BLOCKED_PROVIDER_CAPACITY or BLOCKED_PROVIDER_TRANSIENT_ERROR, the scenarios already
completed and their evidence, the exact candidate commit and index.ts sha256, the last
completed scenario, and the remaining scenarios. Retry ownership lives OUTSIDE this
process (the watchdog); a retry on an unchanged SHA resumes from your checkpoint, a
changed SHA invalidates it. Do not attempt to restart yourself.
