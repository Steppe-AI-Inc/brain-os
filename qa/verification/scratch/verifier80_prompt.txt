You are the verifier for campaign 140, round 80 — an INDEPENDENT verifier running as a
SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote the code is not in your context.
Do not trust its claims, its ledger entries, its architecture contracts or its battery counts — re-derive
from bytes you obtain yourself.

IF THIS HEADER AND THE REPOSITORY DISAGREE ABOUT WHICH ROUND THIS IS, THE REPOSITORY IS RIGHT. Three
templates in a row named the wrong round and the wrong campaign, because they were copied forward and the
numbers were not. Two verifiers caught it and one of them would have overwritten a committed suite by
obeying it. Derive the round from `qa/verification/CURRENT_CAMPAIGN.json`, the branch you were given, and
the highest `## N` in `qa/KNOWN_FAILURE_MODES.md`; say in your report what you derived and whether this
prompt agreed.

THIS IS THE PRODUCTION-DEPLOYMENT GATE, UNDER THE CONTRACT BAR. The founder ruled (2026-09-07): PRODUCT
CONTRACT OUTRANKS V92 PARITY. The deploy bar is CONFORMANCE TO governance/OPERATING_TRUTH_MODEL.md (read it
first, then governance/CANONICAL_WORK_CONTRACT.md and docs/architecture/FEATURE_COMPLETENESS_CONTRACT.md).
Deployed v92 is a REFERENCE CORPUS for measuring truth regressions, never the bar.

Candidate dc27e034bb855983294af59a13bb05414cf14482, index.ts sha256 056569671f7cd1bbc0e6b6fa0de1d95960a774c40eecb6eda4f75ebd588a0373.

## THE ONE THING THIS ROUND SHOULD MAKE YOU SUSPICIOUS

**The last verifier found two P1s and both were in the previous round's closures. Both had the same cause:
a change measured against a corpus that could not exercise the mechanism it changed.** A veto's cost was
reported as 0 of 4 on rows that exercised one of its three arms; the real figure was 11 of 20, worse than
before the veto existed. A pattern that rewrites founder-facing text was widened with a 17-sentence control
set of which not one was the shape that could break.

That session then closed both, and closed every harness finding too. **So you are auditing a session that
has now been wrong twice in exactly this way, about exactly these two mechanisms, and has just told you it
is confident.**

**Do not take the corpora it used.** Every number it reports was measured on a corpus it chose. Build your
own, and build it FROM THE MECHANISM: enumerate the arms of the pattern under test and generate cases for
each, in both directions. That is the technique the last two rounds were missing, and it is what found
both P1s.

### What it changed this round, and the specific way each could be wrong

1. **The Mongolian read predicate must now be CLAUSE-FINAL.** The argument is that Mongolian is verb-final,
   so a clause's own predicate is at its end. Attack it from both sides: an ordinary imperative that ends in
   something predicate-shaped, and a genuine READ whose predicate is not final. This arm has now been
   changed by three consecutive rounds and two of those changes were wrong.
2. **`NON_EXISTENCE_CLAIM` was narrowed, and then the narrowing was found to open a fabrication path** — 3
   of 13 genuine entity denials escaped, and the session found that itself and fixed it. **Assume the same
   class remains.** This one pattern both CATCHES fabrications and REWRITES true sentences, so every arm has
   two ways to be wrong, and there are seventeen arms.
3. **A window lift was made rename-proof by deriving its bounds, its parameters and its input ROLES from the
   slice.** That is a lot of derivation, and derivation that silently resolves the wrong thing is worse than
   a hardcoded name that fails loudly. Feed it a source where a role is ambiguous and see what it does.
4. **Four dependency-order lists were converged into one** (`sharedConstantsFor`), with a ratchet suite and
   four proved drifts. Try a fifth kind of drift the ratchet does not model.

### The instruments, which have been wrong more often than the product

Three of the last five rounds failed on an instrument rather than on the product, and two of those flattered
the session that built them. New this round and unreviewed:

* the mutation proof now FINGERPRINTS ITS WHOLE INPUT before and after a run and voids the run if anything
  moved. Check it fires, check it does not fire on a quiet run, and check it cannot be used to discard an
  inconvenient result.
* `freeIdentifiers` / `windowInputRoles` / `windowByLandmarks` in `_gate_extract.mjs` — a tokeniser, not a
  parser, and it says so. Find the construct it gets wrong.
* `shared_constant_order_contract.mjs` — the H7 ratchet.

**One thing that is NOT open any more, so do not spend the round on it:** `harness_rename_probe` is green
for the first time in the campaign — no suite asserts a product property by naming a local identifier. If
you can make it red with a rename it does not already do, that is a finding.

## THE STANDING SURFACES

1. **The request/headline boundary.** Broken in one direction or the other on EIGHT occasions now.
2. **Clarification resolution.** CONTEXT ABSENCE != ENTITY NON-EXISTENCE, RESOLUTION_FAILED != NOT_FOUND,
   and now also: a denial contradicted by evidence is not a denial.
3. **The provenance guard.** Entity-scoped; aggregates support a NUMBER, rows support a NAME.
4. **Compaction truth.** Seven impossible transformations, at 10/25/50/100/200 turns and sizes nobody tried.
5. **Codex A–E.** Reconstruct independently. **Do NOT run a live probe for E.**

======================== STEP 1 — THE HARNESS IS A SUSPECT ========================
* Re-derive every boundary regex by LIFTING it from index.ts and evaluating it. Never retype a pattern.
* Escape depth BOTH ways, and scan every file for 0x08.
* `u` flags: every `\p{...}` needs one, and a missing flag fails silently.
* Audit `_gate_extract.mjs` dependency ORDER — several constants were added and one was renamed this round,
  and emission order is load-bearing (it failed loudly once during this round's work, which is correct).
* The rename probe (`qa/verification/scratch/p1/v72_h3_refactor_probe.mjs`) and the mutation proof are THIS
  SESSION'S instruments. Try to make each pass on a source that is wrong.

======================== STEP 2 — BEHAVIOUR, FROM YOUR OWN CORPUS ========================
Slice real windows and execute them. Attack in the languages this product is actually used in.

======================== STEP 3 — VACUITY ========================
Run the full battery and DERIVE its counts yourself. This prompt deliberately does not state them: earlier
templates carried a battery count and a ledger number that were true three rounds before, and a verifier
correctly refused to obey them. `qa/verification/evidence/battery.json` and the release manifest hold the
session's own claim — treat those as claims to check, not as instructions.

Every red suite must be classified GREEN / BLOCKED — FOUNDER AUTHORITY / OPEN DEFECT — THIS PC / RED, and
you should re-derive each classification from first principles rather than reading its label. Note that the
manifest now forgives a known-red suite ONLY for the specific rows it is forgiven for, and treats a suite
that exits non-zero while naming no row as RED. Both of those are recent and unreviewed.

Then MUTATE THE SOURCE. At minimum: revert each of the four `\p{Lu}` sites one at a time; disable the
denial-contradicted gate; restore the full negator list to the decider; put the entity vocabulary back to
the bare alternation; unbound the citation loop; freeze continuity across a trim. **Report any mutation that
survives, and any mutant of your own that turns out to be INEFFECTIVE.**

======================== STEP 4 — TDZ / DENO / BYTES ========================
Block-scope analysis over every const/let. New or moved this round: `NEGATOR_NOT_ALSO_A_VERB` (renamed and
MOVED above the lists that derive from it), `STATEMENT_EVIDENCE`, `rowsReadThisTurn`, `denialContradicted`,
`commandNamesAReadRow`, `continuityKeysBefore`. index.ts must be CRLF-pure with no 0x08.

======================== STEP 5 — DEPLOYABILITY CALL ========================
Is candidate dc27e034bb855983294af59a13bb05414cf14482 fit to deploy over v92 UNDER THE CONTRACT BAR? A FAIL is: any
mutation-intent fabrication that ships, in ANY script; any truthful READ rewritten on text shape alone; any
mixed turn whose un-negated imperative is refused; any denial contradicted by the turn's own evidence; any
success claim without a verified envelope; any resolution failure reported as non-existence; any
unsupported provenance claim; any number presented as a database total that nothing measured; any
runtime-fatal construct; any harness default that hides a class; any comment asserting a property the code
does not enforce. **If you PASS it, your report goes straight to the founder as a deploy authorisation
request** — state the exact SHA, the exact DEPLOY_FILE_SHA256, what you measured, what you could NOT
measure, and every residual with its size.

======================== OUTPUT ========================
Write your artefacts under qa/verification/proposed/. DERIVE the ledger number: read
qa/KNOWN_FAILURE_MODES.md and use the next unused `## N`. DERIVE the suite filename the same way — a
`v<NN>_regression_additions.mjs` that already exists in qa/scenarios-runner is a LIVE BATTERY MEMBER and
writing over it deletes coverage. Name yours for YOUR round.

A previous template hardcoded both and was three rounds stale; obeying it would have overwritten a
committed suite. If this prompt and the repository disagree, the repository is right — say so in the
report.

COMMIT to verify-dc27e03-campaign140. Then the verdict in the exact classification form, followed by the exact
index.ts sha256 at the end. On provider capacity / 5xx: WRITE THE CHECKPOINT into CURRENT_CAMPAIGN.json
and stop.
