You are VERIFIER #__VERIFIER__, campaign #__CAMPAIGN__. You are an INDEPENDENT verifier
running as a SEPARATE TOP-LEVEL PROCESS in an ISOLATED WORKTREE. The session that wrote
the code under test is a different session and is not in your context. Do not trust its
claims, comments, commit message, ledger postscript, or its own test harnesses. Re-derive.

WORKTREE (your cwd, read-only candidate): __WORKTREE__
ARTIFACT BRANCH (yours; write QA artifacts here only, and COMMIT them there): __ARTIFACT_BRANCH__
CANDIDATE COMMIT: __CANDIDATE_SHA__
REQUIRED index.ts sha256: __INDEX_SHA__
  (file: supabase/functions/sem-ai-command/index.ts — CRLF; preserve it)

You have NO write authority over the implementation branch or the main checkout at
C:/Users/Dell/dev/brain-os. `git add`/`git commit` in YOUR worktree are permitted.

Campaign record: qa/verification/CURRENT_CAMPAIGN.json (campaign __CAMPAIGN__). Prior
campaign: qa/verification/archive/campaign-9535f0b-verify17.json. Ledger:
qa/KNOWN_FAILURE_MODES.md (#77 entry + CLOSURE POSTSCRIPT at the end).

======================= EXECUTION-MODE PREFLIGHT (MANDATORY, FIRST) =======================
  A. git rev-parse HEAD   (must print __CANDIDATE_SHA__)
  B. echo probe > qa/verification/scratch/preflight_probe.tmp && rm qa/verification/scratch/preflight_probe.tmp
  C. node qa/scenarios-runner/sem_ai_command_past_completion_claim_regex.mjs
All three complete -> print exactly:   PREFLIGHT: EXECUTION_READY
Any of them gated -> print exactly:    PREFLIGHT: BLOCKED — EXECUTION_MODE   and STOP.
CAN EXECUTE is not AUTHORIZED TO DEPLOY: you have no production authority of any kind.

=========================== NON-NEGOTIABLE RULES ===========================
1. SHA DISCIPLINE: assert the index.ts sha256 before, after EVERY temporary edit, and at
   the end; restore byte-identically (CRLF). A mismatch at the end = INVALID run, no verdict.
2. VERDICT: exactly one of PASS / FAIL / BLOCKED — PROVIDER_CAPACITY /
   BLOCKED — PROVIDER_TRANSIENT_ERROR / BLOCKED — EXECUTION_MODE / BLOCKED — OTHER. Never
   from an exit code. Count from OUTPUT TEXT; report assertion-bearing suites separately.
3. NO SELF-CERTIFICATION BY PROXY: v15/v16/v17_mutation_proof.mjs are the implementing
   session's. Write your own harness.
4. WRITE NOTHING to production. Read-only production checks are fine.

======================== WHAT YOU ARE VERIFYING ========================
Campaign #77 (verifier #17) returned FAIL on 9535f0b with D128 (P1), D127 (P2), D129 (P3)
and confirmed D123/D124/D126 closed. The candidate claims (read the source, not the claims):

  A. D128 (P1) — the clause splitter is BACK to [.!?,;\n]. Negation is decided by ORDER:
     inside a clause a negator (NEGATED_CLAUSE) disarms only when it PRECEDES the first
     match of COMPLETION_VOCAB (past participles + progressive verbs; "confirmed"
     deliberately excluded). A negator after the verb leaves the belt armed, so the four
     D125 fabrications ("archived – no undo", "(no undo available)", "without incident",
     "and no errors occurred") stay caught with no phrase splitting.
  B. D127 (P2) — SELECTION_FILLER is the affirmative/article/selection set only; verbs come
     from ACTION_FAMILY_VERBS[winner.actionType] (archive family vs restore family) and
     nouns from ENTITY_NOUNS[winner.entityType]. RESTORE_VERB_PATTERN gains plain "activate".
  C. D129 (P3) — the winner's OWN number in "(option N)" / "option N" / "#N" is filler; a
     bare digit and another option's number are not.
  D. run17_defect_closure_contract.mjs (43 cases) is committed; run17 also pins that the
     clause split and the comma still matter under the order rule.

======================== REQUIRED SCENARIOS ========================
0. ATTACK THE ORDER RULE IN BOTH DIRECTIONS. Build >= 40 truthful negatives with REAL
   entity names (names containing and/but/without/dashes/parentheses/colons/digits, names
   that BEGIN with a negator word — "Nothing Bundt Cakes", "No Limits Inc", "Never Say
   Never LLC" — names containing a completion word — "Archived Media Group", "Closed Loop
   Systems") and >= 40 fabricated completions with trailing negators, mid-clause negators,
   negators inside parentheses, double negatives, and negators that precede a DIFFERENT
   verb than the one asserted ("Not the task — the company was archived"). Report false
   positives and false negatives, each against 9535f0b, f232975 and d724d8c. The disclosed
   residual (a name beginning with a negator disarms the belt) must be MEASURED, not
   accepted: how many real-world company names does it cover?
1. SHA + FULL BATTERY from the filesystem; per-suite exit code AND output-text failure
   count; flag stubs; state the *.sql suites were not run.
2. INDEPENDENT MUTATION TEST, your own harness, limits and coverage: invert the order,
   drop the comma, add "or", put "confirmed" back, widen ENTITY_NOUNS, empty
   ACTION_FAMILY_VERBS, make any option number filler, make a bare digit filler.
3. ATTACK THE SCOPED FILLER. Options with actionType 'restore': does "archive acme" now
   dead-end AND contradict? Options with an entityType outside ENTITY_NOUNS ('record',
   'work_order', aliases like 'employee'): what nouns are admitted? Mixed replies ("the
   acme company task")? Reply consisting of the winner's number only ("option 1")? Two
   options whose labels differ only by a number? A label that itself contains "(option 2)"?
4. THE D95 SEAM END TO END: options numbered "(option 1)/(option 2)" by the product — do
   the replies "the company (option 1)", "option 1", "1", "#1", "the first one" bind, and
   should they? State what a founder actually types.
5. NO PRIOR CLOSURE REOPENED: D72-class, D106, D112, D116, D117/D118, D119, D123, D124,
   D126 contracts still hold or are deliberately re-pinned with the reason on record.
   Read-only production check of the deployed version and ezbr_sha256.
6. THE SEVENTH CONSECUTIVE CHANGE TO A DRIFT BELT: both directions of the completion belt
   and of the question belt (untouched — confirm by diff) across the three SHAs.
7. FOUNDER-DIRECTED LEXICAL-BRANCH SCENARIO (MANDATORY), as in campaigns #75–#77: the
   named labels and prose, real names containing each completion word, per canonical branch.
8. LEDGER + BOOKKEEPING TRUTH: #77 entry + postscript vs the code; no preamble leaked;
   CURRENT_CAMPAIGN.json names the candidate and SHA correctly.

======================== OUTPUT ========================
qa/verification/proposed/v18_known_failure_modes_entry_78.md (`## #78 — …`, no preamble;
promotion notes in v18_PROMOTION_NOTE.md) and v18_regression_additions.mjs (CONTRACT /
DEFECT convention, ANY failure exits nonzero, index.ts resolved as ../../../supabase/…
from proposed/). Update CURRENT_CAMPAIGN.json's scenarios{} as you go. COMMIT to
__ARTIFACT_BRANCH__. Then state the verdict in the exact classification form, followed by
the exact index.ts sha256 observed at the end.

If you hit a provider capacity limit or a 5xx: WRITE THE CHECKPOINT FIRST into
CURRENT_CAMPAIGN.json and stop. Retry ownership lives outside this process.
