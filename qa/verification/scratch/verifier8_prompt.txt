INDEPENDENT ADVERSARIAL VERIFICATION — campaign verify-e8678ec-run7-defect-closure (verifier #8, campaign #68).

Repo: C:/Users/Dell/dev/brain-os, branch pending/d3-past-completion-gate-pendingaction-shortcircuit, commit e8678ec ("Close run7 D50-D54"). You are verifying the fixes for the five defects verifier #7 (campaign verify-25af3b0, record currently in qa/verification/CURRENT_CAMPAIGN.json — archive it to qa/verification/archive/campaign-25af3b0-structured-claim-hardening.json before starting your own record, do NOT destroy it) found in the structured-claim architecture. Production remains on sem-ai-command v92 — you must NOT deploy anything, and all production access is STRICTLY READ-ONLY.

Prior campaign verdict (run7): all eleven #66 laundering shapes closed, but DO NOT DEPLOY because: D50 (P0, evidence at 13/~30 mutating paths + rewrite issues false denial "nothing was changed"), D51 (P0, rewrite discards deterministic backend report; unclaimed real mutations vanish), D52 (P1, one supported mutation claim disarms drift check for unrelated fabricated prose), D53 (P1, questions[]/pendingAction ungated prose channel), D54 (P1, resourceType/action/predicate/expectedValue interpolated raw; expectedValue leaks a real uuid). Also D55 (closed by run7's own suite), D56/D57 (P2, disclosed open).

The implementing session claims e8678ec closes D50-D54. Treat every claim as unverified. Re-derive everything from source and your own adversarial executions. Specifically:

1. D50 coverage: verify recordExecution is genuinely wired at EVERY mutating path (claimed: person end/restore employment, company update, permanent fixture delete incl. cascaded people, channel delete, approval delete, deleteByIds family, department/lead/document/product_line/product_spec/drawing/ai_provider/proposal create+update, provider activate, company_relationship/person_assignment/memory create, factory work_order create). Check each against its real write site — evidence recorded only on genuine success, never on denied/not_found/no-op. Attack: find any mutating path that still records nothing.
2. D50 wording: unsupported claims must never be answered with a denial ("nothing was changed"); only contradicted state claims may be corrected assertively. Attack the wording paths.
3. D51: the deterministic report (factLines/lifecycle reports/org-graph/plan confirm) must survive the rewrite (summaryIsFullyDeterministic/deterministicPrefix computed ABOVE the structured-claim window). Attack: construct turns where deterministic content is lost, duplicated, or misclassified (a turn classified fully-deterministic whose result.summary at that point still contains raw model prose, or vice versa — check every branch that assigns result.summary above the window against the classification expression). Check the harnesses inject these two params faithfully vs what index.ts actually computes.
4. D52: rewrite now triggers on ANY mutation-shaped claim. Attack: find a shape where fabricated completion prose still ships (claims of only state/historical types plus fabricated mutation prose; evidence-without-claims turns; deterministic-mode turns; pendingAction-present turns).
5. D53: safeProseFragment gates questions/proposedActions/pendingAction prompt+options. Attack with fabrications PAST_COMPLETION_CLAIM_PATTERN misses (present tense "is now archived", future promises, Mongolian, markdown), and confirm the persisted envelope (not just summary) is gated.
6. D54: verify all four sanitizers; attack with uuids in every model-controlled field, uppercase/spaced uuids, and check verifyStructuredClaim's REASON strings (which enter the persisted envelope) for uuid/name leakage — decide and record whether envelope reasons count as founder-facing.
7. Test quality: mutation-test the new guards YOURSELF (do not trust the commit's own mutation claims); verify byte-identical restore via sha256 after every mutation; check Section S source-literal checks cannot be satisfied vacuously; check the X-section baseline flips are honest.
8. Regression: full battery (structured_claim_verification, structured_claim_laundering_contract, lifecycle_evidence_and_output_persistence_contract, issue5_confirmation_action_type_binding, sem_ai_command_* truth suites) — all must pass on e8678ec exactly as committed.
9. Production read-only check: confirm v92 still ACTIVE and untouched; zero writes from your session.

Write your campaign record to qa/verification/CURrent_CAMPAIGN.json (correct filename: qa/verification/CURRENT_CAMPAIGN.json) as you go, checkpoint after each scenario. Record new defects with ids D58+. End with an explicit verdict: DEPLOY GATE OPEN (all run7 P0/P1 closed, no new P0/P1) or DO NOT DEPLOY with exact reasons. Your job is to find what the implementing session missed, not to confirm it.

Standing safety rules: no supabase db push, no functions deploy, no production writes, use scripts/safe-function-download.sh if you need the deployed copy, never overwrite the working tree beyond qa/verification/**. Scratch: qa/verification/scratch/.
