# Transport compatibility matrix (WO-2; contract P-3; AC-9) - implementer record, NOT canonical

- Generated by `qa/factory/v1/transport_compatibility_contract.mjs` from `compat_matrix.json`; VERDICT is computed, never hand-written.
- Baseline: `69df2f52f71fd2bc9415c34fb2be4dab4ee08dd6`. Each OLD CALL is checked against that commit's source; each regression ports the certified assertion(s) it names
  (file:line at 69df2f52) and runs through the Node API and through the SQL front doors directly (`qa/factory/v1/compat_regressions.mjs`).
- Migration sha256: `fa3e03bb09190309c18256cab1e94d4787381875677460edf4a34f53d5ab5948`.

| ROW | OLD CALL | OLD DB PRIMITIVE | OLD TRANSACTION BOUNDARY | OLD AUTHORITY CHECK | OLD LEASE / FENCING RULE | NEW API ENDPOINT | NEW SERVER PRIMITIVE (tables written) | NEW AUTHORITY CHECK | REGRESSION TEST (api / direct) | VERDICT |
|---|---|---|---|---|---|---|---|---|---|---|
| register | `scripts/factory-runner/claim.mjs:84` | insert into factory.nodes (node_id, capabilities, security_role, platform, agent_version, last_heartbeat_at) ... on conflict (node_id) do update | one statement | none: the shared factory_runner login; node_id and security_role come from the caller | stamp:false writes the epoch (never beaten) until a claim cycle completes; onlyIfAbsent writes nothing over an existing record | POST /v1/node/register | `factory.node_register`: nodes (runtime_version, runtime_digest, release_id, reported_*, runtime_phase = RECOVERING; never last_heartbeat_at); computer_fingerprints; enrollments + enrollment_transitions (RUNTIME_INSTALLING -> REGISTERING -> ALIVE \| REGISTRATION_FAILED) | session -> credential under a SHARE lock inside the call's transaction (S-3); identity, tenant and envelope from the credential; ALIVE only on a certified release; a body naming identity is refused | `node_truth_acceptance.mjs:1299 N8 / claim.mjs:75 (stamp:false)` PASS / PASS | **MIGRATED** |
| heartbeat | `scripts/factory-runner/node.mjs:75` | update factory.nodes set last_heartbeat_at (when stamping), security_role, capabilities, agent_version | one statement | none; the worker re-asserts its OWN role every beat (N2 required it) | stamps liveness only when admission did not refuse | POST /v1/node/heartbeat | `factory.node_heartbeat`: nodes (last_heartbeat_at, runtime_phase, reported_resources); agent_runs.lease_expires_at = now() for runs not renewed since RECOVERING began (reconciliation) | credential SHARE-locked; the body carries no role, capability or envelope (refused by name): INVERTS 69df2f52 N2 as S-4 requires | `node_truth_acceptance.mjs:201 N2 (INVERTED)` PASS / PASS<br>`node_truth_acceptance.mjs:343 N5` PASS / PASS | **MIGRATED** |
| discover / claim | `scripts/factory-runner/claim.mjs:121` | claimInTransaction (claim.mjs:140): pg_advisory_xact_lock(hashtext('factory.claim')) -> reaper -> read own role/caps -> pick FOR UPDATE SKIP LOCKED -> assurance gate -> insert agent_runs -> authoring_run_id = run_id -> insert surface_locks -> work_orders claimed | one transaction, serialized plane-wide by the advisory lock; 23505 / 54000 / 55P03 = nothing claimed | role and capabilities read from factory.nodes (a row the node writes itself); local admission before any SQL | lease from BEGIN; started_at = clock_timestamp() at the insert | POST /v1/node/claim | `factory.node_claim -> factory._claim`: agent_runs (insert; principal_id, computer_id, credential_id, run_kind, envelope_version, release stamp); surface_locks (insert; principal_id, lease 'infinity'); work_orders.status = claimed; the reaper: agent_runs / work_orders / surface_locks (both fleets) | the SAME advisory lock key as the frozen legacy claim; the 12 hard gates in the founder's order from the credential's CURRENT envelope, then ranking; a lock held past the lock timeout -> claim_lock_busy (nothing claimed); only work orders holding factory-enrolled-v1 | `acceptance.mjs:71 A` PASS / PASS<br>`acceptance.mjs:82 H` PASS / PASS<br>`shared_control_plane_acceptance.mjs:76 CP-3` PASS / PASS<br>`acceptance.mjs:87 I` PASS / PASS<br>`acceptance.mjs:93 I2` PASS / PASS<br>`acceptance.mjs:756 S` PASS / PASS<br>`acceptance.mjs:544 N` PASS / PASS<br>`shared_control_plane_acceptance.mjs:179 CP-9` PASS / PASS<br>`shared_control_plane_acceptance.mjs:183 CP-10` PASS / PASS | **MIGRATED** |
| lease renewal | `scripts/factory-runner/claim.mjs:361` | one CTE: update agent_runs (lease, heartbeat) where run_id and node_id and in_progress -> update surface_locks (lease) -> update nodes (heartbeat) | one statement | node_id self-asserted | fence: run_id = $1 and node_id = $2 and status = 'in_progress'; false = taken over (LOST) | POST /v1/node/renew | `factory.node_renew`: agent_runs (last_heartbeat_at, lease_expires_at); nodes.last_heartbeat_at | credential SHARE-locked; the same fence on the credential's own node id; a refused renewal is audited (lease_lost). An enrolled lock carries lease 'infinity': its lifetime is its run's lease, so renewing the run renews the lock | `node_truth_acceptance.mjs:343 N5` PASS / PASS<br>`acceptance.mjs:515 M` PASS / PASS | **MIGRATED** |
| checkpoint | `scripts/factory-runner/claim.mjs:387` | fence read -> insert into checkpoints ... on conflict (checkpoint_id) do nothing -> update agent_runs checkpoint fields | three statements | node_id self-asserted | a run that is no longer this node's writes no checkpoint (LeaseLost); the caller's checkpoint id makes a retry write one row | POST /v1/node/checkpoint | `factory.node_checkpoint`: checkpoints (insert on conflict do nothing; principal, computer, release stamp); agent_runs (checkpoint_location, last_completed_scenario, branch, worktree) | the fence INSIDE the front door, on the locked run row, in the same transaction as the insert | `acceptance.mjs:101 C` PASS / PASS<br>`acceptance.mjs:102 D` PASS / PASS<br>`node_truth_acceptance.mjs:628 N26` PASS / PASS<br>`acceptance.mjs:515 M` PASS / PASS | **MIGRATED** |
| complete | `scripts/factory-runner/claim.mjs:420` | one CTE: update agent_runs (fenced) -> delete surface_locks -> update work_orders status | one statement | node_id optional (null = unfenced) | fenced on live ownership: a superseded run returns {superseded:true}; a failed run fails its work order | POST /v1/node/complete | `factory.node_complete`: agent_runs (terminal fields; lease null); surface_locks (delete); work_orders (done \| failed \| review + verification_state, current_candidate_run_id); work_orders (insert: exactly one verification work order); audit_events | credential SHARE-locked; ALWAYS fenced on the credential's node id and the run's live ownership; terminal reason required; no silent substitution; a verification-required work order moves to WAITING_FOR_INDEPENDENT_VERIFICATION, never done; a resubmitted FAILED tree is refused | `acceptance.mjs:156 M2` PASS / PASS<br>`acceptance.mjs:166 M3` PASS / PASS<br>`acceptance.mjs:179 M4` PASS / PASS<br>`acceptance.mjs:196 M5` PASS / PASS<br>`acceptance.mjs:262 J` PASS / PASS<br>`acceptance.mjs:619 P` PASS / PASS<br>`node_truth_acceptance.mjs:316 N4` PASS / PASS<br>`node.mjs:514 (landed check) / node_truth_acceptance.mjs:628 N26` PASS / PASS | **MIGRATED** |
| surface-lock acquire | `scripts/factory-runner/claim.mjs:329` | insert into surface_locks per surface; the PRIMARY KEY is the enforcement | inside the claim transaction | none | 23505 = the other node won; a repeated surface is one lock | POST /v1/node/claim | `factory._claim`: surface_locks (insert; principal_id; lease_expires_at 'infinity' so the frozen legacy pick always sees it live) | gate 8 reads live locks of BOTH fleets (a legacy lock by its lease; an enrolled lock by its run's lease); the primary key still enforces | `acceptance.mjs:76 B` PASS / PASS<br>`acceptance.mjs:87 I` PASS / PASS<br>`acceptance.mjs:93 I2` PASS / PASS<br>`acceptance.mjs:756 S` PASS / PASS | **MIGRATED** |
| surface-lock release | `scripts/factory-runner/claim.mjs:420` | delete at completion; delete expired at claim; lease_expires_at = now() at abort / orphan give-back | inside completion / claim / give-back | none | locks die with their run's lease | POST /v1/node/complete; POST /v1/node/release; (the reaper inside POST /v1/node/claim) | `factory.node_complete / factory.node_release / factory._reap`: surface_locks (delete at completion and certification; the reaper deletes lapsed legacy locks and enrolled locks whose run is not live); agent_runs.lease_expires_at = now() (give-back) | credential SHARE-locked; give-back only of this node's own runs | `acceptance.mjs:266 J2` PASS / PASS<br>`node_truth_acceptance.mjs:501 N13 / node.mjs:283` PASS / PASS<br>`node_truth_acceptance.mjs:722 N29 / node.mjs:244` PASS / PASS | **MIGRATED** |
| takeover / recovery | `scripts/factory-runner/claim.mjs:168` | update agent_runs set status = 'queued', lease_expires_at = null, attempt_count + 1 (node kept) where the lease lapsed -> work_orders claimed -> queued -> delete expired locks | inside the claim transaction | none | only a REAL lease expiry makes work claimable again; the stale owner is fenced by renew / checkpoint / complete | POST /v1/node/claim (the reaper) | `factory._reap + factory._claim`: agent_runs / work_orders / surface_locks (the same statements, both fleets); work_orders.verification_state (a lapsed verification claim returns to WAITING); agent_runs.resumed_from_checkpoint_id (the checkpoint handed to the new owner) | as claim; the new owner must pass every gate; the reaper's effects commit with the call (work still becomes claimable only after a real expiry) | `acceptance.mjs:112 E/G` PASS / PASS<br>`acceptance.mjs:116 G2` PASS / PASS<br>`acceptance.mjs:120 G3` PASS / PASS<br>`shared_control_plane_acceptance.mjs:97 CP-5` PASS / PASS<br>`acceptance.mjs:515 M` PASS / PASS | **MIGRATED** |
| verification claim | `scripts/factory-runner/claim.mjs:140` | inside the claim: the role-rank filter on requires_security_role and the model-assurance gate; the verify work order points at its run through handoff JSON | inside the claim transaction | the node's self-written role | as claim | POST /v1/node/verification-claim | `factory.node_verification_claim -> factory._claim('verification')`: agent_runs (run_kind verification); work_orders.verification_state = VERIFICATION_CLAIMED | gate 5 (verifier in the CURRENT envelope) and gate 7 (the S-13 floor against the whole authoring set; the campaign's physical separation). The 69df2f52 model-assurance decline is not one of contract P-6's twelve gates; the enrolled runtime keeps it as its own local veto (it does not ask for verifier work its model may not serve) | `shared_control_plane_acceptance.mjs:179 CP-9` PASS / PASS<br>`shared_control_plane_acceptance.mjs:210 CP-11` PASS / PASS | **MIGRATED** |
| certification | `scripts/factory-runner/claim.mjs:48` | update agent_runs set verification_run_id, verification_node_id where run_id ...; CHECKs verification_is_independent / verification_node_is_independent | 2-3 statements | not fenced to the caller; any verificationRunId accepted | only a done run is verifiable | POST /v1/node/certify | `factory.node_certify`: certifications (insert; its own table); agent_runs (the verification run done); work_orders (the verification work order done; the certified work order COMPLETE or VERIFICATION_FAILED -> queued); audit_events | fenced on the certifier's own verification run; the S-13 floor in the front door whatever the policy rows say; exact provenance of the CURRENT candidate; verifier authority in the CURRENT envelope; the policies can only add | `shared_control_plane_acceptance.mjs:210 CP-11` PASS / PASS<br>`node_truth_acceptance.mjs:1122 N12` PASS / PASS | **MIGRATED** |
| abort give-back | `scripts/factory-runner/node.mjs:283` | update agent_runs set lease_expires_at = now() where run_id and node_id and in_progress; the same on surface_locks | two statements, best effort | node_id self-asserted | an aborted run is recoverable at once | POST /v1/node/release | `factory.node_release`: agent_runs.lease_expires_at = now(); work_orders.verification_state (a released verification claim -> WAITING) | credential SHARE-locked; only this node's runs | `node_truth_acceptance.mjs:501 N13 / node.mjs:283` PASS / PASS | **MIGRATED** |
| orphan give-back | `scripts/factory-runner/node.mjs:244` | one CTE: runs of this node in progress with a live lease not held by this process -> lease_expires_at = now(); their locks likewise | one statement | node_id self-asserted | gives back claims whose COMMIT reply was lost | POST /v1/node/release (keep_run_ids) | `factory.node_release`: agent_runs.lease_expires_at = now() for every run of this node not named in keep_run_ids | credential SHARE-locked; only this node's runs | `node_truth_acceptance.mjs:722 N29 / node.mjs:244` PASS / PASS | **MIGRATED** |
| completion-landed check | `scripts/factory-runner/node.mjs:514` | read the run's status and node after a completion whose reply was lost | a separate read | none | a retried completion that landed is recognized | POST /v1/node/complete | `factory.node_complete`: nothing on a retry: the answer says superseded with landed = true | as complete | `node.mjs:514 (landed check) / node_truth_acceptance.mjs:628 N26` PASS / PASS | **MIGRATED** |
| run setup | `scripts/factory-runner/node.mjs:461` | read the work order's type, title, handoff, surface, required role; update the run's worktree, branch, base_commit | separate statements, retried | none | none | POST /v1/node/claim (the work order in the answer); POST /v1/node/checkpoint (branch, worktree) | `factory._claim / factory.node_checkpoint`: agent_runs.base_commit at the claim; branch / worktree at a checkpoint | as claim / checkpoint; no separate read (no route outside S-7) | `acceptance.mjs:71 A` PASS / PASS<br>`acceptance.mjs:102 D` PASS / PASS | **MIGRATED** |
| status / health reads | `scripts/factory-runner/node.mjs:556` | pg_stat_ssl, the node's row, schema / role reads, a deliberate DDL probe | reads | none | read-only | GET /v1/time; the answers of register / heartbeat (draining, release_current, revocations, rotate_required); the Admin API's computer view | `factory.node_time / factory.node_heartbeat / factory.admin_get_computer`: nothing (heartbeat stamps liveness only) | the node reads only its own state, through its own calls; liveness is derived, never stored (contract §1) | `node_truth_acceptance.mjs:1299 N8 / claim.mjs:75 (stamp:false)` PASS / PASS | **MIGRATED** |

Rows MIGRATED: 16 of 16.
