{
  "_doc": "Passed to every director via --mcp-config. Declared EXPLICITLY rather than inherited: the Playwright server is registered in the user config against the parent directory, not the repo, so an unattended director could otherwise start with no browser and still report UI results. Regenerated by qa/runner/lib/config.mjs.",
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ],
      "env": {}
    }
  }
}
