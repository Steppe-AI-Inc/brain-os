window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.aiBackend = (()=>{ const U=SEM.Utils; return { render(){ const s=SEM.Store.get(); return `<div class="grid grid2"><div class="card"><h3>Real AI Backend Connection</h3><p class="muted">API keys must live in serverless/backend environment variables, never in browser JavaScript.</p><div class="formGroup"><label>AI Mode</label><select id="aiMode"><option value="fallback">Fallback local logic — zero API tokens</option><option value="real">Real AI backend — serverless endpoint</option></select></div><div class="formGroup"><label>Backend endpoint</label><input id="aiEndpoint" value="${U.esc(s.settings.aiEndpoint)}" /></div><div class="formRow"><div class="formGroup"><label>Small model label</label><input id="modelSmall" value="${U.esc(s.settings.modelSmall)}" /></div><div class="formGroup"><label>Medium model label</label><input id="modelMedium" value="${U.esc(s.settings.modelMedium)}" /></div></div><div class="formGroup"><label>Strong model label</label><input id="modelStrong" value="${U.esc(s.settings.modelStrong)}" /></div><button class="primary" id="saveAI">Save AI settings</button><button style="margin-left:8px" id="testAI">Test endpoint</button><div id="aiTest" style="margin-top:12px"></div></div><div class="card"><h3>Backend files included</h3><div class="list"><div class="item"><b>Vercel</b><p><code>api/ai-command.js</code> and <code>api/health.js</code></p></div><div class="item"><b>Netlify</b><p><code>netlify/functions/ai-command.mjs</code></p></div><div class="item"><b>Supabase Edge Function</b><p><code>supabase/functions/sem-ai-command/index.ts</code></p></div><div class="item"><b>Environment variable</b><p><code>OPENAI_API_KEY</code> must be set in hosting dashboard.</p></div></div></div></div>`; }, afterRender(){ const s=SEM.Store.get(); U.$('#aiMode').value=s.settings.aiMode; U.$('#saveAI').onclick=()=>{ Object.assign(s.settings,{aiMode:U.$('#aiMode').value, aiEndpoint:U.$('#aiEndpoint').value.trim(), modelSmall:U.$('#modelSmall').value.trim(), modelMedium:U.$('#modelMedium').value.trim(), modelStrong:U.$('#modelStrong').value.trim()}); SEM.Store.save(); U.toast('AI backend settings saved'); }; U.$('#testAI').onclick=async()=>{ const endpoint=U.$('#aiEndpoint').value.trim(); U.$('#aiTest').innerHTML='<div class="item">Testing...</div>'; try{ const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({command:'Health check. Return one simple task.',contextPack:{},tokenPlan:{route:'small',model:s.settings.modelSmall,totalTokens:1000}})}); const txt=await res.text(); U.$('#aiTest').innerHTML=`<pre class="code">${U.esc(txt.slice(0,2000))}</pre>`; }catch(e){ U.$('#aiTest').innerHTML=`<div class="item"><b class="dangerText">Endpoint test failed</b><p>${U.esc(e.message)}</p></div>`; } }; }} })();
