window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.integrations = (()=>{
  const U=SEM.Utils;
  function renderPending(kind, items){ return items.map(a=>`<div class="item"><div class="itemHeader"><b>${U.esc(a.title||a.text||a.type)}</b>${U.statusPill(a.status)}</div><p class="muted">${kind} · ${U.esc(a.createdAt||'')}</p>${a.channel?`<span class="pill blue">${U.esc(a.channel)}</span>`:''}</div>`).join('')||'<p class="muted">No pending actions.</p>'; }
  return { render(){ const s=SEM.Store.get(), slack=s.integrationSettings.slack, drive=s.integrationSettings.googleDrive; return `<div class="grid grid2"><div class="card"><h3>Slack Connector</h3><p class="muted">Use Slack for team updates, blocker capture, approval alerts, and daily digests. This browser MVP queues Slack actions; production sends through a secure backend.</p><div class="formRow"><div class="formGroup"><label>Status</label><select id="slackStatus"><option ${slack.status==='not_connected'?'selected':''}>not_connected</option><option ${slack.status==='configured'?'selected':''}>configured</option><option ${slack.status==='connected'?'selected':''}>connected</option></select></div><div class="formGroup"><label>Default channel</label><input id="slackChannel" value="${U.esc(slack.defaultChannel||'#sales')}" /></div></div><div class="formGroup"><label>Workspace</label><input id="slackWorkspace" value="${U.esc(slack.workspace||'')}" placeholder="SEM / OpenSpot workspace" /></div><button class="primary" id="saveSlack">Save Slack settings</button><button class="ghost" id="queueSlackDigest" style="margin-left:8px">Queue daily digest</button><hr><h3>Pending Slack Actions</h3><div class="list">${renderPending('Slack', slack.pendingActions||[])}</div></div><div class="card"><h3>Google Drive Connector</h3><p class="muted">Use Google Drive for proposals, quotations, specs, contracts, and current knowledge docs. Production should use limited file-level access where possible.</p><div class="formRow"><div class="formGroup"><label>Status</label><select id="driveStatus"><option ${drive.status==='not_connected'?'selected':''}>not_connected</option><option ${drive.status==='configured'?'selected':''}>configured</option><option ${drive.status==='connected'?'selected':''}>connected</option></select></div><div class="formGroup"><label>Folder name</label><input id="driveFolderName" value="${U.esc(drive.folderName||'SEM Brain Proposals')}" /></div></div><div class="formGroup"><label>Folder ID</label><input id="driveFolderId" value="${U.esc(drive.folderId||'')}" placeholder="Google Drive folder id" /></div><label class="pill"><input id="driveFileScope" type="checkbox" ${drive.useDriveFileScope?'checked':''} style="width:auto" /> Prefer limited drive.file scope</label><div class="actions" style="margin-top:12px"><button class="primary" id="saveDrive">Save Drive settings</button><button class="ghost" id="queueKnowledgeExport">Queue knowledge export</button></div><hr><h3>Pending Drive Exports</h3><div class="list">${renderPending('Drive', drive.pendingExports||[])}</div></div></div><div class="grid grid2" style="margin-top:18px"><div class="card"><h3>Production Backend Checklist</h3><ul><li>Slack OAuth install flow and bot token stored server-side.</li><li>Slack approval alerts use backend-only token and approval gates.</li><li>Google OAuth is server-side with least-permission scopes.</li><li>Google Docs generation runs after proposal approval.</li><li>Every outbound message/export creates an audit log.</li></ul></div><div class="card"><h3>Automation Use Cases</h3><div class="list"><div class="item"><b>Slack → SEM Brain</b><p class="muted">Employee status updates become tasks, blockers, KPI evidence, and manager summaries.</p></div><div class="item"><b>SEM Brain → Slack</b><p class="muted">Daily digest, missing updates, approvals, proposal-ready alerts.</p></div><div class="item"><b>SEM Brain → Google Drive</b><p class="muted">Approved quotations, proposals, PRDs and meeting notes become organized Drive documents.</p></div></div></div></div>`; }, afterRender(){ U.$('#saveSlack').onclick=()=>{ const s=SEM.Store.get(); s.integrationSettings.slack={...s.integrationSettings.slack,status:U.$('#slackStatus').value,defaultChannel:U.$('#slackChannel').value.trim(),workspace:U.$('#slackWorkspace').value.trim()}; SEM.Store.save(); U.toast('Slack settings saved'); SEM.App.render(); }; U.$('#queueSlackDigest').onclick=()=>{ const s=SEM.Store.get(); s.integrationSettings.slack.pendingActions.unshift({id:U.uid('slack'),type:'daily_digest',channel:s.integrationSettings.slack.defaultChannel||'#general',text:'SEM Brain daily digest: approvals, blockers, high-priority tasks, missing updates.',status:'queued',createdAt:U.now()}); SEM.Store.save(); U.toast('Slack digest queued'); SEM.App.render(); }; U.$('#saveDrive').onclick=()=>{ const s=SEM.Store.get(); s.integrationSettings.googleDrive={...s.integrationSettings.googleDrive,status:U.$('#driveStatus').value,folderName:U.$('#driveFolderName').value.trim(),folderId:U.$('#driveFolderId').value.trim(),useDriveFileScope:U.$('#driveFileScope').checked}; SEM.Store.save(); U.toast('Google Drive settings saved'); SEM.App.render(); }; U.$('#queueKnowledgeExport').onclick=()=>{ const s=SEM.Store.get(); s.integrationSettings.googleDrive.pendingExports.unshift({id:U.uid('gdrive'),type:'knowledge_backup',title:'SEM Brain knowledge memory export',status:'queued',createdAt:U.now()}); SEM.Store.save(); U.toast('Knowledge export queued'); SEM.App.render(); }; }};
})();
