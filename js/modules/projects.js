window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.projects = (()=>{ const U=SEM.Utils; return { render(){ const s=SEM.Store.get(), user=SEM.Store.currentUser(); const rows=s.projects.filter(p=>SEM.Permissions.canSeeCompany(user,s.companies.find(c=>c.id===p.companyId)||{})); return `<div class="card"><h3>Projects</h3><table class="table"><thead><tr><th>Project</th><th>Company</th><th>Owner</th><th>Deadline</th><th>Risk</th><th>Blockers</th></tr></thead><tbody>${rows.map(p=>`<tr><td><b>${U.esc(p.title)}</b><br><span class="muted">${U.esc(p.goal)}</span></td><td>${U.esc(s.companies.find(c=>c.id===p.companyId)?.name||'')}</td><td>${U.esc(s.people.find(x=>x.id===p.ownerPersonId)?.fullName||'')}</td><td>${U.esc(p.deadline||'')}</td><td>${p.riskScore}</td><td>${U.esc(p.blockers||'')}</td></tr>`).join('')}</tbody></table></div>`; }} })();
