window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.tasks = (()=>{ const U=SEM.Utils; return { render(){ const s=SEM.Store.get(), user=SEM.Store.currentUser(); const rows=s.tasks.filter(t=>SEM.Permissions.canSeeCompany(user,s.companies.find(c=>c.id===t.companyId)||{}) || user.personId===t.ownerPersonId); const statuses=['queued','in_progress','needs_approval','done']; return `<div class="kanban">${statuses.map(st=>`<div class="col"><h4>${st.replace('_',' ')}</h4>${rows.filter(t=>t.status===st).map(t=>`<div class="taskCard"><b>${U.esc(t.title)}</b><p class="smallText">${U.esc(t.description||'')}</p><div class="badgeBar">${U.statusPill(t.priority)}${U.statusPill(t.riskLevel)}${t.approvalRequired?'<span class="pill red">approval</span>':''}</div><p class="smallText">Owner: ${U.esc(t.ownerPersonId?s.people.find(p=>p.id===t.ownerPersonId)?.fullName:s.agents.find(a=>a.id===t.ownerAgentId)?.name || '-')}</p></div>`).join('') || '<p class="muted">Empty</p>'}</div>`).join('')}</div>`; }} })();
