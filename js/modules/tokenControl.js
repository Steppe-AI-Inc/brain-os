window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.tokenControl = (()=>{ const U=SEM.Utils; return { render(){ const s=SEM.Store.get(); const usage=SEM.TokenBudget.usageSummary(); const dayPct=Math.min(100, usage.dailyTokens/(s.settings.dailyTokenBudget||1)*100); const monthPct=Math.min(100, usage.monthlyTokens/(s.settings.monthlyTokenBudget||1)*100); return `<div class="grid grid3"><div class="stat"><div class="label">Daily usage</div><div class="num">${usage.dailyTokens.toLocaleString()}</div><div class="progress"><div style="width:${dayPct}%"></div></div></div><div class="stat"><div class="label">Monthly usage</div><div class="num">${usage.monthlyTokens.toLocaleString()}</div><div class="progress"><div style="width:${monthPct}%"></div></div></div><div class="stat"><div class="label">Est. monthly cost</div><div class="num">$${usage.monthlyCost.toFixed(3)}</div><span class="pill green">forecast only</span></div></div><div class="grid grid2" style="margin-top:18px"><div class="card"><h3>Token budget settings</h3><div class="formRow"><div class="formGroup"><label>Daily token budget</label><input id="dailyBudget" type="number" value="${s.settings.dailyTokenBudget}" /></div><div class="formGroup"><label>Monthly token budget</label><input id="monthlyBudget" type="number" value="${s.settings.monthlyTokenBudget}" /></div></div><div class="formRow"><div class="formGroup"><label>Hard stop per command</label><input id="hardStop" type="number" value="${s.settings.hardStopTokensPerCommand}" /></div><div class="formGroup"><label>Approval threshold</label><input id="approvalThreshold" type="number" value="${s.settings.approvalThresholdTokens}" /></div></div><button class="primary" id="saveTokenSettings">Save budgets</button></div><div class="card"><h3>Estimate command cost</h3><textarea id="tokenCmd" placeholder="Paste founder command to estimate..."></textarea><button id="tokenEstimate" class="primary">Estimate</button><div id="tokenResult" style="margin-top:12px"></div></div></div><div class="card" style="margin-top:18px"><h3>Token event log</h3><table class="table"><thead><tr><th>When</th><th>Route</th><th>Tokens</th><th>Cost</th><th>Command</th></tr></thead><tbody>${s.tokenEvents.slice(0,20).map(e=>`<tr><td>${U.esc(e.createdAt)}</td><td>${U.statusPill(e.route)}</td><td>${Number(e.totalTokens||0).toLocaleString()}</td><td>$${Number(e.estimatedCostUsd||0).toFixed(5)}</td><td>${U.esc(e.command||'')}</td></tr>`).join('')}</tbody></table></div>`; }, afterRender(){ const s=SEM.Store.get(); U.$('#saveTokenSettings').onclick=()=>{ Object.assign(s.settings,{dailyTokenBudget:+U.$('#dailyBudget').value,monthlyTokenBudget:+U.$('#monthlyBudget').value,hardStopTokensPerCommand:+U.$('#hardStop').value,approvalThresholdTokens:+U.$('#approvalThreshold').value}); SEM.Store.save(); U.toast('Token budgets saved'); SEM.App.render(); }; U.$('#tokenEstimate').onclick=()=>{ const cmd=U.$('#tokenCmd').value; const ctx=SEM.ContextPack.build(cmd); const plan=SEM.TokenBudget.estimateCommand(cmd,{category:SEM.Pipeline.categoryOf(cmd),riskLevel:SEM.Pipeline.riskOf(cmd),contextPack:ctx}); U.$('#tokenResult').innerHTML=`<pre class="code">${U.esc(JSON.stringify(plan,null,2))}</pre>`; }; }} })();
