window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.agents = (()=>{ const U=SEM.Utils; return { render(){ const s=SEM.Store.get(); return `<div class="card"><h3>AI Agent Managers</h3><table class="table"><thead><tr><th>Agent</th><th>Role</th><th>Skills</th><th>Cost limit</th><th>Status</th></tr></thead><tbody>${s.agents.map(a=>`<tr><td><b>${U.esc(a.name)}</b></td><td>${U.esc(a.role)}</td><td>${(a.skills||[]).map(x=>`<span class="pill blue">${U.esc(x)}</span>`).join(' ')}</td><td>$${a.costLimitUsd}</td><td>${a.active?U.statusPill('active'):U.statusPill('paused')}</td></tr>`).join('')}</tbody></table></div>`; }} })();
