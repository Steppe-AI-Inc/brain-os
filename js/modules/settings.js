window.SEM=window.SEM||{}; SEM.Modules=SEM.Modules||{};
SEM.Modules.settings = (()=>{ const U=SEM.Utils; return { render(){ return `<div class="grid grid2"><div class="card"><h3>System Settings</h3><button class="danger" id="resetApp">Reset local workspace</button><p class="muted">Export backup before resetting.</p></div><div class="card"><h3>Version</h3><p><b>SEM Brain MVP ${U.esc(SEM.Store.get().version)}</b></p><p class="muted">Real AI backend ready. Modular static frontend. Token-limited pipeline.</p></div></div>`; }, afterRender(){ U.$('#resetApp').onclick=()=>{ if(confirm('Reset local SEM Brain data?')){SEM.Store.reset(); U.toast('Reset complete'); SEM.App.render();} }; }} })();
