.vercel
supabase/.temp/
node_modules/
.env

# Machine-local factory node state. A node id is generated per CHECKOUT: committing one would
# make every clone claim to be the same node, which breaks lease ownership and surface locks.
.factory/
.factory-rehearsal/
.claude/settings.local.json

# Build outputs (scripts/factory-build/build-sea.mjs writes dist/brain-factory/<version>/). Anchored: only the root dist/.
/dist/
